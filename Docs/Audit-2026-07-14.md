# TimeTracker 全面审核报告

审核启动：2026-07-14

实现/文档快照：2026-07-19
状态：重构实现与 R1 收口已完成，主动重构停止；未执行的完整发行矩阵保留为明确的未声明范围

本文把“审核起点发现”“已实施修复”“已有定向证据”和“收口证据/未声明范围”分开。起点的 P0/P1 是 baseline finding，不是当前仍未处理的 blocker；没有取得的完整测试、设备或 trace 证据明确写为未执行，不伪装成通过，也不自动变成继续重构的待办。

## 1. 执行摘要

本轮不是单点修补。审核覆盖主应用、SwiftData/CloudKit、App Intents、Live Activity、Widget、Watch、三语言资源、测试与文档，并允许 UI、操作流程、代码和同步结构发生 breaking change。

主要结果：

- iPhone 改为系统五标签；iPad/macOS 使用原生 split navigation；macOS 使用一个主 `Window`，Settings 共享应用 store。
- Today、任务详情、分析、设置、番茄钟、开始计时、时间线菜单、编辑器丢弃确认和辅助功能信息层级进行了全局调整。
- API key 移入 device-only Keychain；AI 配置改为 Test→Save 草稿，自动建议默认关闭并需另行同意。
- Inbox/checklist AI 投影共用 UTF-8 字段、48 候选/16 KiB JSON、24 KiB prompt 和 32 KiB request body 上限；Inbox 优先固定与高频/近期任务，完整本机 symbol picker 与 78 项 AI 语义集分离，结果 UUID/icon 必须属于实际已发送集。
- Demo 默认关闭并使用独立 store；生产 store 永不物理 purge tombstone。
- Task hierarchy 改为 `parentID` 权威、canonical path 和启动/恢复 repair，消除深层路径写放大与异常图风险。
- 完成任务现在保留在任务树和历史中，但与后代一起阻止新增工作；归档分支继续隐藏。重新打开会一次恢复路径上的完成阻塞项，主应用与系统入口共用同一 trackable 判定。
- Forecast 优先使用当前任务明确的预计时长，没有预计时长时才回退到 checklist 完成证据和真实计时；父子预测分别归属，90 日 pace 不生成剩余工时。
- SwiftData 当前 schema 为 V11（`1.10.0`）：V8→V9 lightweight migration 移除可从 ledger 重建的持久 `DailySummary` cache，V9→V10 custom migration 为 Inbox item/suggestion 初始化不透明 context/revision identity 并保留旧 dismissal 语义，V10→V11 lightweight migration 增加带 caller-owned external key 的 Inbox capture durable receipt。
- legacy Countdown 本机迁移限制 256 KiB JSON、256 条源记录、4 KiB UTF-8 标题与有限的 `[1900-01-01, 2201-01-01)` 日期；保留合法 UUID，重复 UUID 只取第一条合法记录，实际导入在 SwiftData save 成功后才设置完成 flag 并删除旧 payload。
- Pomodoro 由持久 phase 状态派生 deadline；segment edit/delete、timer stop 和 task-tree delete 保持 run/session/segment 一致。
- App Intent 提交后刷新 Widget/Watch/Live Activity，但投影失败不改变 durable mutation 结果。
- Deep link 在执行和启动排队前使用同一严格 parser；待处理队列按语义去重并限制 16 项。Watch 进程级 callback 通过弱引用 router 选择最近活跃 scene，避免旧 `ContentView`/store 泄漏或误路由。
- Watch 使用持久命令队列、typed terminal result、durable ack、20 秒 timeout、同 ID retry/discard；手机在 mutation 前拒绝超过 30 秒的旧命令，避免离线命令迟到后意外改写当前计时。
- Watch command/result/snapshot codec 与 iPhone/Watch 恢复队列增加统一结构、字节、数量、时间、唯一 ID 和 encoded-size 上限；unsafe restore 清除，pending overflow 转成明确 `queueOverflow` failure，持久队列不再可无限增长。
- Widget/Watch producer 用 Unicode-safe prefix、summary/start clamp、count cap 和 128 KiB 文本预算生成有界投影。Widget App Group 快照又在 save/load 两侧 fail closed：JSON 256 KiB、64 active/64 recent、UTF-8/时间/统计/唯一 ID 受限，损坏数据不再被误解为空账本。
- SyncConflict state 使用跨进程 file lock；Cloud export 使用 epoch/generation/fingerprint checkpoint，抵抗 lost update 与乱序回调。
- Ledger、Checklist、Rollup 实现 scoped/incremental refresh；完整历史累计保持精确，forecast pace 只统计最近 90 个本地日；50,000 segment 预算保护单记录 mutation。
- 手工补录时间也在 store-scoped fresh-context 事务内再次核对任务可计时性，避免跨窗口/同步刚完成、归档或删除任务后把记录写入旧归属。
- 同步偏好和 Countdown 新建已不再使用 scene-owned context；LLM 完整配置与 API key 更新在同一 store lock 内串行，继续通过明确补偿而非伪 ACID 处理 Keychain/SwiftData 的失败边界。
- Watch store 已拆成 base、Commands、Connectivity 和 SessionDelegate 四个文件，分别拥有 observable state/restore、command queue/timeout/persistence、WCSession transport/payload/freshness 与 delegate callbacks；ledger flat-array index 与 rollup scoped mutation 也已从各自主 owner 拆出，并由 source-layout contract 约束不回退。
- Analytics overview/task snapshot 按 period 与 active-only minute bucket 缓存，重计算移出 SwiftUI `body`；同一周期的 revision/live-bucket 刷新保留正确的已显示内容并给出轻量进度，不同周期绝不复用旧统计。
- R1 baseline：签名 macOS `CorePerformanceBudgetTests` 初始 8/8 成功；720 条月度 Analytics 用例总计 98 ms、2,000 条高重叠 Today 用例总计 284 ms（均含 fixture 构造）。R1 已把 Today 的小时活动、timeline 与 overlap 移到 Sendable value input 的 cancellable worker；首轮 2,000 条拆分测量的 main projection `< 50 ms`、visual 后 main core assembly 145.17 ms，并由 175 ms residual budget 守护。最终定向签名回归 139/139 成功，完整 `CorePerformanceBudgetTests` 为 9/9 成功；universal macOS Release archive 已成功，保留 `Apple Development: ZEXUAN GAO (PX46M259V3)` / team `LT98S43NKA`。archive 唯一 warning 是 `timetrackerWatchApp` 未设置 `LSApplicationCategory`；这是非 R1 的 release-metadata 待决项，已记录但不扩展为主动重构。它不是“整份 snapshot 已后台化”的夸张声明。Release XCTest 当前 testability 配置不能形成有效数据，不以禁用 signing 或无限等待规避。实现与验证由 `55f19ae` 提交，提交后工作树和本批运行资源均已清理。
- 工程保持 Automatic Signing 和团队 `LT98S43NKA`，没有使用禁用签名绕过能力问题。

### Breaking 变更与兼容边界

| 范围 | Breaking 变化 | 当前兼容/恢复边界 |
| --- | --- | --- |
| UI 信息架构 | 旧 iPhone 自绘六目的地 chrome、卡片式 Today 和内联任务大编辑器不再保留；改为五个系统标签、原生 split navigation 和只读优先详情 | 稳定的任务 UUID、deep link 和 shared selection 仍路由到同一详情；旧截图与点击位置不能作为自动化合同 |
| 操作流程 | 开始计时总是先显示可搜索任务选择器；Pomodoro 计划/任务改为显式菜单；设置重新分组，Mac Settings 改为标准独立场景 | 隐藏点击、静默复用旧任务、把详情页当编辑器等旧流程有意移除；行为测试与 accessibility identifier 是新合同 |
| 任务工作流 | `completed` 不再等同隐藏：完成分支可浏览但不能接收新工作；`archived` 继续隐藏。重新打开会恢复所选路径上的完成阻塞项 | 任务与历史 UUID 保留；所有计时、手工记录、Pomodoro、Inbox、Intent 和 create/move 入口使用同一 trackable 规则 |
| 预测语义 | 任务明确预计时长从纯 metadata 改为自身 forecast 首选来源；没有预计时长时使用 checklist 证据回退 | 预计时长只覆盖当前任务直接工作，子任务另行汇总；旧正值上限规范化为 10 小时，零视为未设置；90 日 pace 不创造工时 |
| 数据语义与 schema | `TaskNode.parentID` 成为层级权威，`path` 从祖先链改为稳定 `/<UUID>` locator；当前 schema 为 V11，V9 移除持久化 `DailySummary` 派生 cache，V10 增加 Inbox suggestion 不透明 identity，V11 增加外部 Inbox capture durable receipt | V8→V9 lightweight、V9→V10 custom 与 V10→V11 lightweight migration 保留任务、账本和其他用户事实；legacy 类型继续供迁移读取；异常 orphan/cycle 按确定性规则提升为可用根节点 |
| 同步与冲突状态 | 同步状态加入 optional epoch/generation/fingerprint checkpoints、跨进程锁和权威 state/mirror 规则；旧的“任一 export 成功即视为已同步”语义被移除 | optional state 字段允许旧状态解码；损坏文件隔离后由受控 mirror/新状态恢复。真实多设备和混合版本仍是发行门禁 |
| 删除与隐私 | 生产 store 不再物理 purge tombstone；“清空全部数据”除逻辑删除业务事实外，还清除本机 LLM Keychain 密钥和自动建议同意 | iCloud 启动开关保持设备本地且不被重置动作暗改；JSON 仍不可作为恢复备份 |

允许 breaking change 不等于允许静默数据丢失。任何未来模型形状变化仍须按 [AD-007](AgentDecisions.md) 增加 schema 版本、真实旧 store fixture、幂等迁移和回退说明。

## 2. 审核范围与方法

| 范围 | 核对内容 |
| --- | --- |
| UI/UX | iPhone/iPad/Mac 导航，Today、Inbox、Tasks、Task Detail、Pomodoro、Analytics、Settings，Dynamic Type、VoiceOver、触控目标、错误/空状态、编辑器安全 |
| 领域正确性 | gross/wall/overlap、跨午夜/DST、任务树、并行 timer、Pomodoro ledger、删除与 LWW/tombstone |
| 数据与同步 | SwiftData schema/migration、CloudKit 模式、冲突快照、强制上传/下载、跨进程 state、App Group |
| 系统表面 | App Intents、Live Activity、Widget、WatchConnectivity、deep link、post-commit projection |
| 安全/隐私 | API key、Keychain、endpoint/redirect、导出过滤、Privacy manifest、第三方 AI 数据边界 |
| 性能 | Observation fan-out、轮询、range fetch、incremental indexes、Analytics cache、task hierarchy、50k budget |
| 工程质量 | 文件职责、行为/源码契约测试、本地化、签名、文档一致性、FlowDown 参考价值 |

方法包括全库静态阅读、定向单元/构建批次、签名设置与 entitlement 检查、模拟器/界面检查，以及对 FlowDown 的定向代码参考。最终 runtime 证据必须在源码冻结后重新取得，历史批次不相加冒充一次全套通过。

## 3. Baseline P0/P1 发现与处置

| 起点优先级与发现 | 当前处置 | 仍需发行验证 |
| --- | --- | --- |
| P0：LLM API key 曾处于普通偏好/同步/导出边界 | **代码与文档已解决**：device-only、non-synchronizable Keychain；遗留迁移后清理；快照/导出过滤；网络 redirect fail closed | 冻结工作树 Keychain/迁移/过滤套件，Archive 与日志 secret scan |
| P0：隐私清单与发行声明不完整 | **工程侧已处理**：主 App、Widget、Watch 有 manifest；UserDefaults reasons 分别为主 App `1C8F.1`/`CA92.1`、Widget `1C8F.1`、Watch `CA92.1`；主 App/Widget/Live Activity 的手写 Info.plist membership exception 已核对；数据流文档已补 | Archive 中的合并 manifest、Live Activity 是否确实无需独立 manifest、App Store 标签与 AI endpoint 保留披露 |
| P1：Widget 缺少可靠 App Group 发行证据 | **代码/签名配置已解决**：主 App 与 Widget 使用 `group.me.mezorewww.timetracker`，快照错误可诊断 | 真机共享容器读写、timeline reload、锁屏、离线、升级；不能由模拟器代替 |
| P1：红色/脆弱测试削弱回归信号 | **大量行为与契约已修复**，关键领域有定向通过记录；源码字符串契约持续减少 | 当前最终工作树完整 unit/UI 串行复验仍待回填 |
| P1：JSON 被误解为备份、旧 store fixture 不完整 | **产品表述已解决**：统一称 JSON 导出，明确无 importer/checksum/rollback | importer、校验、staging/rollback、真实旧版本 fixtures 仍是未来能力 |
| P1：UI 层级混乱、隐藏交互、非原生根导航 | **已进行 breaking redesign**：五标签/split view/read-first detail/明确菜单/可搜索 picker/原生列表与设置 IA | 冻结工作树正常字号 iPhone/iPad/Mac 核心路径、平台 HIG、键盘与真机验收；只有文本重排或语义风险触发时才追加 VoiceOver/极端字号专项 |

因此，早期 P0/P1 不再作为“尚未编码处理”的阻断项。表中最后一列是发行证据或明确未实现的产品能力，不应与代码修复状态混淆。

## 4. UI/UX 全局重构

### 导航与信息架构

- iPhone 删除自绘六目的地 chrome，使用 Today、Inbox、Tasks、Focus、Analytics 五个系统 `Tab`；Settings 从 Today 工具栏进入。
- iPad 在所有窗口宽度都保持同一个 `NavigationSplitView`；分屏、Stage Manager 和旋转只触发系统列折叠/显示，不切换成 iPhone 五标签根导航。macOS 继续使用 `NavigationSplitView`。
- macOS 主场景由 `WindowGroup` 改为单实例 `Window`。主窗口与标准 Settings scene 共享一个 `TimeTrackerStore`，避免多个窗口重复安装 CloudKit observers、自动 AI 和系统表面同步。
- Task Detail 是统一的只读/执行/证据页面，完整编辑通过明确铅笔 sheet 进入。

### Today、任务与时间线

- Today 改为按优先级排列的原生列表：Active Timers、今日摘要、Quick Start、可解释预测、时间线、倒计时。
- “开始计时”在所有平台都打开可搜索任务选择器，显示标题、父级路径和运行状态；任务身份先占满横向空间，Start/Switch 与 Running/Stop 位于独立命令行，不再和长标题互相挤压，也没有历史选择导致的静默启动/无反馈。
- 时间线条目主体进入任务详情，尾部独立 44 pt 省略号菜单承载编辑、补录相似记录和删除；上下文菜单保留作为次级入口。
- Today、Quick Start、Pomodoro、新记录与 Inbox 建议统一使用任务可计时判定，归档分支不再从某个入口漏出；活动任务树的归档动作要求先停止计时，历史时间片仍保留原任务归属。
- 任务/分类/Quick Start/Inbox 建议/手工时间/segment 等有草稿的编辑器在关闭前检查未保存更改。
- 任务颜色通过 light/dark 动态对比适配；关键 rows、chart、selection、Widget、Live Activity 与 Watch 补充可访问性语义。
- 移除没有证据支持的 `CADisableMinimumFrameDurationOnPhone` Info.plist 覆盖；iPhone 刷新率交给系统，流畅度以 Release runtime 和真实设备 trace 判断。

### Pomodoro

- 计划与任务是两个可见菜单，不再依赖点击标题/计时器的隐藏手势。
- Setup 已拆为组合容器、空状态、focus 参数控件、Plan/Task 选择与 timer face，不再由一个视图同时承担全部交互和展示。
- 活动界面拆分为可编译、可滚动的原生 SwiftUI 结构；状态、剩余时间、轮次、开始下一轮与停止动作明确。
- Focus deadline 在后台/启动时对账；break 不自动创建下一轮，避免用户未同意时后台生成计时事实。

### Analytics 与 Settings

- Analytics 首页先显示当前范围摘要；“复盘”按 Decisions、Quality 排列，“深入查看”按 Time、Tasks、Pomodoro、Metrics 排列。六个 typed destination 继续进入独立原生列表详情，原 Overview 用户文案已改为 Metrics。
- Landing page 与 category detail 已分成独立所有者；`AnalyticsCategory` 使用 typed navigation value/destination，避免在 root 中内联所有六类详情 composition。
- 重计算由 request key 驱动的 `.task(id:)` 执行；只有与活动 segment 相交的当前范围按分钟更新。
- 同一日、周或月的 revision/live-bucket 刷新不再把整个分析页替换为 spinner；不同日、周或月仍只显示新周期的 loading 内容，避免指标与控制器的 period 语义错配。
- 小时图提供 hour/gross/wall/primary task 的 VoiceOver 表达；辅助功能字号下范围控件改用菜单。
- Settings 分为通用、专注、数据与同步、AI 助手、高级；compact iOS 使用 sheet 适配，不强行显示窄 popover。
- 共享 Settings rows 拆为 foundation/value、action/destructive、input、presentation 与 sync-feedback 五个所有者；辅助功能字号重排、VoiceOver title/value/state 与破坏性行的显式红色文字/图标在共享层统一。
- AI editor 使用 draft → Test → model selection → Save；取消有改动草稿会确认。自动建议是另一个明确开关。

完整操作见 [用户手册](UserGuide.md)，设计边界见 [Agent 决策 AD-012/019/027](AgentDecisions.md)。

## 5. 代码正确性、同步与性能重构

### 时间账本与 Pomodoro 一致性

- Today gross/wall/average focus 使用本地日区间裁剪，跨午夜记录不再整段落入错误日期。
- `TrackedTimePolicy` 统一裁剪所有已记录时长到 reference `now`；本地新增/编辑拒绝未来时间，CloudKit/导入/历史时钟偏差记录保留但不提前计入。
- Manual/segment editor 的 DatePicker、时长与保存验证共用该 policy；`TrackedTimeDisplaySnapshot` 统一 Today timeline、Task Detail recent records 和共享 duration label，已结束 label 不再无意义每秒刷新。
- `PomodoroRun.phaseDeadline` 由持久 phase start 和计划时长派生；过期 focus 在 deadline 截断 segment/session，不把应用挂起时间算作专注。
- 编辑活动 Pomodoro segment 会重绑 task/start；关闭会完成或取消；删除会 tombstone run/session；删除任务树会停止该树所有 timers、结束 active/break Pomodoro，并保留历史。
- 所有多实体用户动作继续由 `ModelContext.performAtomicMutation` 统一提交或 rollback。

### 任务树与删除语义

- `parentID` 是任务层级权威，`TaskNode.path` 固定为 `/<UUID>`；显示路径由标题即时构建，最多显示最近六级。
- `TaskHierarchyMetadataService` 迭代修复 orphan/cycle、depth 与 canonical path；启动、任务刷新、sync restore 均执行 normalization。
- 任务移动不再因祖先链 path 改变而重写整个深子树。
- 生产 Local/iCloud/fallback/emergency store 永不物理 purge tombstone。只有隔离 Demo/UI Test store 可以清理过期 tombstone graph；可见 orphan 不因 staged import 暂缺父记录而删除。

### 同步、跨进程与系统表面

- Cloud/remote 通知经过 350 ms 合并；启动和前台刷新保留，无常驻 5 秒轮询。
- 同步状态不再用本机 refresh 时间冒充 CloudKit 完成：import/export/setup 只有在系统事件成功且本机刷新、冲突处理全部成功后才显示最近活动；remote-store 信号不生成绿色状态，CloudKit 或本机后处理错误保留为失败结果。
- legacy `UserDefaults` 偏好迁移以 logical-key LWW winner 判断已迁移 key；winning tombstone 不再被过滤掉，旧本机值不能复活已删除偏好。
- `SyncConflictState.json` 的 read-modify-write 在递归进程锁与 POSIX `lockf` file lock 内完成，防止主应用与 Shortcuts lost update；它现在是小型 V1 manifest，完整 local/forced-upload/cloud/working snapshot 只保存在受控、hash 校验的 A/B slot。
- 本地同步文件读写限制为 manifest 128 MiB、每个 snapshot slot 与 recovery mirror 64 MiB；读取经 metadata 与 `FileHandle limit + 1` 双重检查，写入在任何路径解析/建目录/authority 替换前预检所有新 slot、manifest 与所需 mirror。slot 先 durable publish、manifest 后发布；任一写 payload 超限都保留旧的有效 authority。
- manifest 引用的 slot 缺失、解码失败、长度或 SHA-256 不符时，slot 与 manifest 一起隔离并显式进入恢复；旧内联 state 在首次 locked read 迁移，不把残缺数据降级为无冲突。
- local generation、sync epoch 和 export event checkpoint 绑定 fingerprint；failed/stale/out-of-order finish 不会回退 base 或清除较新的 forced-upload winner。checkpoint 限 16 个/24 小时。
- 修复旧 state 清理被排除偏好后的延迟回调竞态：清理会重算 fingerprint 并作废清理前 payload 的在途 checkpoints，旧 export completion 不能再把未过滤 base 写回。
- 修复损坏 pending forced-upload mirror 阻塞主库的问题：mirror 会单独隔离并被忽略；只有损坏权威 state 进入显式恢复流程。
- Snapshot restore 在原子 mutation 前对记录数、重复 UUID、字段/总文本 UTF-8 预算、日期、sort order、enum、Pomodoro 计划、偏好 JSON 和可证明的 session/task 矛盾做纯预检；非法 payload 整体拒绝，不静默去重/钳制，也不改写现有记录或 tombstone。缺失关联可兼容 staged CloudKit import。该边界仅覆盖显式 snapshot restore，不代表 SwiftData/CloudKit 直接 materialize 的初始 import 经过同一预检。
- App Intents 共用应用容器与领域命令。提交后用窄 lifecycle 刷新 Widget/Watch/Live Activity 和恢复 snapshot；projection 失败不会把已持久化的非幂等动作返回为失败。
- Deep-link parser 拒绝超长、credential、port、fragment、重复/未知 query 和非法 UUID；启动前 pending queue 有界、去重并在 scene teardown 清空。Watch command route 不再由 singleton closure 强持有某个 `ContentView` store。
- Watch queue 持久化；command/result 都有 durable 通道；typed terminal status、20 秒 timeout、30 秒 command-age guard、同 ID/新时间 retry、discard 和旧 snapshot-reflection compatibility 已实现。过期命令返回 invalid 且不写 receipt/ledger。
- Watch snapshot/result/command payload 验证有限日期、UTF-8 长度、summary/active-age 边界和唯一 ID；state snapshot 限 64 active/256 recent，iPhone incoming 与 Watch pending/failed 队列限 64 项，encoded queue 限 512 KiB，非法恢复 fail closed。
- Widget snapshot producer 限制数量、安全裁剪 Unicode 投影文本、summary 与 timer start，并保持 encoded JSON `<= 256 KiB`；Watch producer 保持 64 active/256 recent 与 128 KiB 总文本预算。Widget store 在 encode 前后验证字段、时间、summary、数量与唯一 ID，保存超限明确失败，读取超限/非法返回 corrupted。
- 修复 companion surface 的陈旧时间误导：Widget 与 Watch 的 elapsed 仅在当前 snapshot 时实时增长，stale/clock-adjusted 时冻结在 `generatedAt`；Dynamic Island compact trailing 复用 Live Activity 的 stale policy。`quickStartTaskIDs` 的 preference refresh 也会立即重新发布 Watch snapshot，避免 Watch 首页继续显示旧快捷任务。
- Timer、Watch、Shortcut、Pomodoro start/break-resume 与手工 Segment 编辑的 parallel admission 不再由 scene/Watch/App Intent 的缓存偏好决定：store lock 内的 fresh `SyncedPreference` 是唯一准入来源；Watch post-commit refresh 和同步记录使用锁内实际 mutation outcome，而非旧投影预猜 events。

### 增量计算与缓存

- Ledger 初次加载建立 segment ID/day/active/array/session indexes；范围 mutation 只 fetch/replace 相交记录及相关 session，并输出 coalesced delta。
- Ledger/Rollup index 额外跟踪 active + future-ended time-sensitive rows；时钟正常前进时局部更新，clock rewind 时全量重评，保持与 full rebuild 一致。
- 今日平均专注时长按当天范围索引查询，Pomodoro session 累计按 session index 查询，不再为两个 read model 扫描全部历史 segment。
- Checklist task-scoped refresh 只替换相关 items/visuals，避免每次 toggle 重建全库分组。
- Rollup 使用 segment before/after、scoped checklist 和 task/ancestor IDs 增量更新；完整历史 worked seconds 精确。
- Forecast pace 只保留包含今天的最近 90 个本地 Calendar 日，并只对有记录的活跃日求平均；它只将已有 remaining seconds 换算为预计活跃日。
- Analytics overview/task caches 按 range、真实 period start、task ID 与可选 active minute bucket 建 key；ledger mutation 清除 snapshots 并按相交区间失效 day bucket。
- 任务树读取路径由 mutation-owned read index 与容量四的 revision-aware projection cache 提供；旧的 624 行聚合服务按 model、index、cache、repair、遍历与组装职责拆为 7 个 50–121 行文件，并由每文件不超过 160 行的结构合同防止回退。
- Task checklist progress 和 frequent recent ranking 使用已维护索引；50,000 segment 性能测试比较单记录增量结果与全量重建，并保护 cached ranking。

仍未处置的性能高风险（2026-07-16 静态复审，不表示已经修复）：

- P0：iCloud/staging 模式的每次本机 mutation 仍在 MainActor 内加跨进程锁、读取/解码最高 128 MiB 单体 state、抓取变更域、对完整 snapshot 编码/哈希、原子重写 state，随后 `prompt()` 又读取刚写文件。应先让 mutation 直接返回 prompt，随后把元数据与分域快照拆开，并用串行后台 actor 保序、合并和 flush；用 1/10/50 MiB state 与 1k/10k/100k segment 基准约束主线程提交延迟。
- P1：task domain refresh 连续发布 tasks/categories/assignments，导致树/read-index/forecast 多轮重建；task 与 ledger refresh 又会重复执行 relationship visibility 全历史扫描。应提供单次 domain snapshot 发布，并让 full refresh 复用 canonical task IDs、只在关系变化时更新 visibility。
- P1：计时开始/层级 mutation 多次抓整棵任务树；Inbox reorder 为 1+N fetch 并重写全部 open rows；Widget 与 Watch 对相同 task/ledger 事件重复排名、today totals 和 snapshot 投影。应让一次命令复用权威 snapshot、Inbox 使用一次 logical-winner 解析与稀疏排序、系统表面共享一次 projection inputs。
- P1：lifetime ledger 全量常驻并同时保存在数组、字典、ID sets、day/task/session indexes 与 rollup snapshot，内存随历史无界增长。长期方案是热窗口 + 持久化日/任务汇总 + 按范围分页；短期先建立 10k/100k/1m segment 的启动峰值和 RSS 门槛。
- P2：Checklist reorder 仍全表 fetch；Inbox 与 Home body 内重复 filter/sort/ranking；手动 JSON export 在 MainActor 抓全域并 pretty encode。分别改为 predicate/变化项写入、revision-keyed display projection，以及后台可取消的流式文件导出。

2026-07-17 已处置的正确性/反馈项：数据库清理改为 throwing 并用只读 SwiftData store 验证最终 save 失败不误报 `0`；无法兑现的“立即刷新同步”按钮/摘要已删除，设置页只保留 typed iCloud 账户可用性检查；账户检查不再推进 cloud activity 时间，失败/不可用会覆盖旧绿色状态；JSON export、数据库清理和同步恢复的失败已归属发起 Settings scene，普通成功改为 section 内联。

仍未处置的正确性问题（2026-07-17 复审）：畸形 Countdown/偏好迁移仍可能退休源数据；cloud smoke test catch 后仍可能成功退出；stale category ID、repository missing target 与空 App Intent 可被报告为成功 no-op；合法 JSON 但字段不完整的 pending conflict 仍可能被当作无冲突；其他 Settings mutation、编辑器与后台健康路径仍有部分依赖共享 `errorMessage`。后续必须逐项改为 throwing/typed outcome 并补失败保持/不误报测试，不能用过渡桥掩盖。

### Demo、安全与隐私

- Debug/Release 自动 demo mode 均为 `off`；明确开启后使用无 CloudKit 的 `TimeTracker-Demo.store`，UI test 使用内存 store。
- API key 使用 `kSecAttrAccessibleAfterFirstUnlockThisDeviceOnly` 且 `kSecAttrSynchronizable=false`；旧明文迁移后删除。
- LLM endpoint、模型列表和已选模型由一个批量 preference command 做一次 SwiftData 提交。Keychain 不属于该 ACID transaction；偏好提交失败时应用尽力恢复旧密钥，并把补偿失败作为独立错误报告。
- “清空全部数据”同时清除 Keychain API key 与设备本地自动建议同意；SwiftData 清理失败时尽力恢复这些外部存储值。当前设备的 iCloud 启动开关不在重置范围内。
- iOS 同步权威状态、pending forced-upload 恢复镜像和腐损隔离文件使用 `completeUntilFirstUserAuthentication`，兼顾首次解锁前的保密性与解锁后后台 Shortcuts/CloudKit 可用性。
- 远程 HTTP、伪装 loopback、跨源/端口 redirect 和 HTTPS downgrade 被拒绝。
- AI 自动建议默认关闭且仅本机；发行前仍须确认实际 endpoint 的运营方、用途、保留/训练/删除政策。

## 6. FlowDown 参考结论

参考仓库：[Lakr233/FlowDown](https://github.com/Lakr233/FlowDown)；本地路径：`/Users/aac6fef/Developer/FlowDown`；审核快照 `694ba5d`。

采用的是模式，不是依赖：

- 显式 RecoveryMode、诊断和恢复入口。
- 未来 importer 所需的版本、checksum、预检、staging/rollback 思路。
- 多 target xctestplan 与 App Intent/Live Activity 测试组织。
- 只有 profiling 证明稳定顺序 + 高频键查找成为瓶颈时，才评估 OrderedCollections。

当前不引入 FlowDown 的 UIKit ListViewKit/AlertController、SnapKit、ColorfulX、SPIndicator 或大批传递依赖，因为原生 SwiftUI 可以表达当前交互，新增依赖会扩大二进制、许可证、供应链、隐私 manifest 和多平台维护面。截至 2026-07-15，工程没有因这次参考新增 Swift Package 依赖，也没有生成对应的 `Package.resolved`。该决定见 [AD-011](AgentDecisions.md)。

## 7. 已有定向证据（不是最终全套）

这些结果只证明取得证据时的对应切片：

- 2026-07-14 核心定向 9 suites：94/94 通过（LLM、forecast、Analytics、ledger、LWW、maintenance、sync、performance 等当时切片）。
- 2026-07-14 command/system action/Watch/sync 4 suites：50/50、0 failed、0 skipped；xcresult：`/tmp/timetracker-post-refactor-audit/Logs/Test/Test-timetracker-2026.07.14_13-44-38-+0800.xcresult`。
- Watch typed-result/durable-ack 实现批次：`CoreWatchCommandTests` 22/22；Watch target 以 Apple Development 身份签名构建通过。后续共享树变更仍要求最终复验。
- 2026-07-15 Watch payload/queue hardening 定向批次：`CoreWatchCommandTests` 26/26；签名 watchOS build 通过。该批次证明 codec/恢复边界与编译签名，不代表配对 Watch/iPhone 的真实 transport、离线和功耗验收。
- 2026-07-15 Widget snapshot validator 定向批次：`CoreWidgetSnapshotTests` 10/10；付费 Apple Development 签名 Widget build 通过。该批次证明 App Group snapshot store/validator 与扩展编译签名；producer shaping 由下一批次补足，两者都不是真机共享容器、timeline reload、锁屏或离线验收证据。
- 2026-07-15 snapshot producer shaping 定向签名批次：`CoreWidgetSnapshotTests` + `CoreWatchCommandTests` 38/38；xcresult：`/tmp/timetracker-snapshot-producer-4-20260715.xcresult`；Team `LT98S43NKA`，signer `Apple Development: ZEXUAN GAO`。该批次证明 Unicode-safe shaping、count/text/JSON 预算、summary/start clamp 与签名构建，不代表配对 Watch/iPhone transport 或 Widget 真机 App Group/timeline 验收。
- 2026-07-15 Pomodoro/ledger/lifecycle 定向签名批次：61/61、0 failed、0 skipped；xcresult：`/tmp/timetracker-pomodoro-lifecycle-4.xcresult`；Team `LT98S43NKA`，identity `Apple Development: ZEXUAN GAO (PX46M259V3)`。这只证明该批次覆盖的启动/前台恢复、后台过期、账本编辑/删除、任务树删除与多活跃 run 对账切片。
- 2026-07-15 sync/source-layout 定向签名批次：59/59（`CoreSyncConflictTests` 36、`CoreSourceLayoutTests` 23），0 failed；xcresult：`/tmp/timetracker-sync-final-20260715-1.xcresult`；Team `LT98S43NKA`，identity `Apple Development: ZEXUAN GAO (PX46M259V3)`。签名 app 包含 CloudKit `iCloud.me.mezorewww.timetracker`、App Group `group.me.mezorewww.timetracker` 与 APS development entitlement；仍不是完整 suite 或 Release archive 证据。
- 2026-07-15 Watch/Ledger/Rollup 结构与 Project Map 定向签名批次：`CoreSourceLayoutTests` 29/29，0 failed、0 skipped；xcresult：`/tmp/timetracker-structure-docs-20260715.xcresult`；Team `LT98S43NKA`，identity `Apple Development: ZEXUAN GAO (PX46M259V3)`，签名产物通过 `codesign --verify --deep --strict`。该批次证明三组职责拆分、文件存在/大小预算和 Project Map 入口契约，不替代 Watch transport、ledger/rollup 行为测试或最终完整 suite。
- 2026-07-15 future-time integrity 付费签名定向批次：86/86、0 failed，并通过三语 localization parity；xcresult：`/tmp/timetracker-future-time-focused-20260715.xcresult`。该批次只证明 `TrackedTimePolicy`、repository 写入拒绝、聚合/缓存/增量索引与 clock rewind 的定向切片，不是当前工作树的完整 suite 或模拟器 UI 证据。
- 2026-07-15 future-time UI 定向批次：4/4；付费 Apple Development 签名构建通过，CloudKit、App Group 和 APS entitlements 保留。该批次只覆盖 editor/timeline/recent/shared-duration 契约与编译签名，不替代最终模拟器/真机人工验收。
- 2026-07-15 Settings shared-row 定向批次：4/4；付费 Apple Development 签名构建通过，CloudKit、App Group 和 APS entitlements 保留。该证据只覆盖共享行的结构/辅助功能契约与编译签名，不替代最终 Settings 模拟器/真机人工验收。
- 2026-07-15 preference tombstone migration 定向批次：14/14；xcresult：`/tmp/timetracker-persistence-audit.xcresult`。该批次只证明 legacy `UserDefaults` 迁移与 logical-key LWW/tombstone 回归切片，不代表当前工作树的完整 suite 或 UI 验收。
- 2026-07-15 local sync/recovery file-bound 定向批次：40/40；xcresult：`/tmp/timetracker-local-file-bounds-20260715.xcresult`。该批次只证明本地 state/recovery 大小上限、超限隔离与相关 sync 回归切片，不代表完整 suite 或 UI 验收。
- 2026-07-15 local sync write-bound 正式回归：73/73、0 failed、0 skipped；xcresult：`/tmp/timetracker-sync-state-write-regression-20260715.xcresult`。覆盖主状态超限、mirror 超限、精确边界、独立 mirror rewrite 与旧文件不变，并使用 Team `LT98S43NKA` / `Apple Development: ZEXUAN GAO (PX46M259V3)` 签名；仍不替代冻结源码后的完整 suite。
- 2026-07-15 snapshot restore preflight 回归：78/78、0 failed、0 skipped；xcresult：`/tmp/timetracker-sync-snapshot-preflight-final2-20260715.xcresult`。其中 9 项新测试覆盖记录/文本预算、重复 UUID、日期/sort order、raw value、Pomodoro、preference JSON、关联矛盾、staged 缺失关联与失败时 sentinel/tombstone 不变；合并执行了相关 sync/layout 回归。使用 Team `LT98S43NKA` / `Apple Development: ZEXUAN GAO (PX46M259V3)` 签名；只证明显式 snapshot restore 切片，不代表初始 CloudKit import、完整 suite 或 UI 验收。
- 2026-07-15 AI request projection + sync isolation warning 联合定向回归：41/41、0 failed、0 skipped；xcresult：`/tmp/timetracker-llm-input-sync-warning-20260715.xcresult`。覆盖 Inbox/checklist 候选/字段/prompt/body 预算、大任务库优先级、curated/full symbol 分离、输出归一化、三语键、旧 LLM 边界与 sync state write tests。xcresult 记录 0 build/runtime warning，产物通过 `codesign --verify --deep --strict`，Team `LT98S43NKA`，identity `Apple Development: ZEXUAN GAO (PX46M259V3)`。该批次不代表 LLM 真实服务隐私/保留政策、完整 suite 或 UI 验收。
- 2026-07-15 preference migration atomicity 定向回归：20/20、0 failed、0 skipped；xcresult：`/tmp/timetracker-preference-migration-atomic-20260715.xcresult`。真实只读磁盘 store 证明普通偏好迁移保存失败会 rollback pending rows、保留源值且不设置完成 flag；敏感 redaction 保存失败不会半改 SwiftData，同时保留已建立的 Keychain 安全副本。xcresult 0 build/runtime warning，签名产物通过严格验证，Team `LT98S43NKA`，identity `Apple Development: ZEXUAN GAO (PX46M259V3)`。
- 2026-07-15 preference batch validation 定向回归：主边界批次 22/22（`/tmp/timetracker-preference-validation-20260715.xcresult`），其余偏好/迁移/LLM 设置/本地化相关批次 42/42（`/tmp/timetracker-preference-related-20260715.xcresult`）；两份 xcresult 均 0 failed/skipped/build/runtime warning。新增行为覆盖 raw `null`/畸形/错类型/超 256 KiB 拒绝、后项失败整批不变、canonical 值、真实只读 store rollback 与 legacy 超限不写 `null`。产物严格签名验证为 Team `LT98S43NKA` / `Apple Development: ZEXUAN GAO (PX46M259V3)`；两批是独立切片，不相加冒充完整 suite。
- 2026-07-15 DeviceIdentity validation 定向回归：5/5，0 build/runtime warning；xcresult：`/tmp/timetracker-device-identity-tests.xcresult`。有效当前平台 canonical UUID 稳定复用；跨平台、超限、控制字符、畸形/非规范值重建。generic iOS（含 Watch/扩展）签名构建和 macOS/iOS 严格 codesign 通过，Team `LT98S43NKA`。
- 2026-07-15 LLM response transport 定向回归：隔离实现批次 45/45（`/tmp/timetracker-llm-response-final.xcresult`），合并后错误优先级与本地化复验 36/36（`/tmp/timetracker-llm-error-priority-20260715.xcresult`）；均 0 failed/skipped/build/runtime warning。覆盖 ephemeral/no-cache/no-cookie、60 秒资源 timeout、2 MiB headers/实际流式上限、非 2xx 不读正文、取消传播、typed timeout、注入 transport 二次防御与 HTTP 状态优先。Apple Development 签名和严格 codesign 通过；两批仍不是冻结工作树完整 suite。
- 2026-07-15 AI model ID/snapshot compatibility 定向回归：19/19、0 failed、0 skipped；xcresult：`/tmp/timetracker-model-id-restore-20260715.xcresult`。新增契约验证 256-byte model ID 原样保留、超限 Unicode ID 按完整字符有界化，并能通过 snapshot restore preflight。xcresult 0 build/runtime warning，签名产物通过严格验证，Team `LT98S43NKA`，identity `Apple Development: ZEXUAN GAO (PX46M259V3)`。
- 2026-07-15 opaque model identity 定向回归：44/44、0 failed/skipped/build/runtime warning；xcresult：`/tmp/timetracker-model-identity-policy-20260715.xcresult`。偏好、网络投影和 snapshot compact-field 统一为 256 UTF-8 bytes；精确边界保留，超限 Unicode 与控制字符模型 ID 整体拒绝/过滤而非截断碰撞。使用 Team `LT98S43NKA` 自动签名；仍是定向切片。
- 2026-07-15 Inbox capture draft retention 定向回归：最终重跑 16/16、0 failed、0 skipped；xcresult：`/tmp/timetracker-inbox-draft-retention-rerun-20260715.xcresult`。验证 store 真实返回提交状态、失败/未配置时保留原草稿、成功后才清空，以及 capture row 不再自行无条件清空 binding；xcresult 0 build/runtime warning，Team `LT98S43NKA`，identity `Apple Development: ZEXUAN GAO (PX46M259V3)`，严格签名验证通过。首次运行只有一条过时的源码字符串契约失败，不计为通过；更新为新行为契约后完成上述重跑。
- 2026-07-15 legacy Countdown migration 定向批次：原输入边界批次 6/6（`/tmp/timetracker-legacy-countdown-bounds-retry-20260715.xcresult`）；加入只读 store 保存失败回归后 7/7、0 failed、0 skipped（`/tmp/timetracker-legacy-countdown-save-failure-rerun-20260715.xcresult`）。后者证明失败时 migration flag 与 legacy payload 保持可重试，并使用 Team `LT98S43NKA` / `Apple Development: ZEXUAN GAO (PX46M259V3)` 签名；仍不代表完整 suite 或 UI 验收。第一次只读内存夹具因 `/dev/null` 无权限在容器加载阶段失败，不计为行为测试通过。
- 2026-07-16 Today/Tasks Accessibility 信息完整性批次：付费 Apple Development 签名的 macOS 定向套件 50/50（`/tmp/timetracker-accessibility-context-final-20260716.xcresult`）；iPhone 17 Pro / iOS 27 的 Accessibility XXXL Today、Tasks、Settings 串行 UI 用例 3/3（`/tmp/timetracker-accessibility-context-ui-20260716.xcresult`）。三张截图从 xcresult 导出到 `/tmp/timetracker-accessibility-context-ui-images-20260716` 并目视通过；专用 UDID `9CEA8CAE-F2B6-4AE7-B092-DEFB389653F4` 已终止、关闭、删除，资源审计为空。自动化覆盖 accessibility label/value，但不替代真人 VoiceOver 遍历，也不代表冻结源码全套。
- 2026-07-16 Widget/Live Activity 动作与 stale 语义批次：签名定向套件 39/39（`/tmp/timetracker-widget-liveactivity-semantics-20260716.xcresult`）；generic iOS 主 App、Widget、Live Activity、Watch 自动签名构建与 embedded validation 通过（`/tmp/timetracker-widget-liveactivity-signed-build-20260716.xcresult`）。签名审计同时发现源文件误用 `com.apple.developer.aps-environment`，导致旧构建虽成功但最终 App 丢失 APS entitlement；修正为规范 `aps-environment` 后，契约测试 1/1（`/tmp/timetracker-aps-entitlement-contract-20260716.xcresult`）和重新签名构建（`/tmp/timetracker-aps-entitlement-signed-build-20260716.xcresult`）通过，源 plist、`.xcent`、embedded profile、最终 signature 四层均为 development，并保留 CloudKit、App Group、Team `LT98S43NKA`。这仍不是 App Group/APS/ActivityKit 真机通信证据。
- 2026-07-17 companion stale-time 定向批次：`CoreWidgetSnapshotTests`、`CoreWatchCommandTests`、`SystemSurfaceInteractionContractTests` 与既有 surface contract 共 56/56、0 failed；macOS 自动签名身份为 Team `LT98S43NKA` / `Apple Development: ZEXUAN GAO (PX46M259V3)`。首次运行由新 stale guard 的反向条件捕获并失败，修正后完整重跑通过；未创建模拟器，两个本批 xcresult 均已删除，结束时无 owned `xcodebuild`、`xctest` 或受测 App。这证明 elapsed freeze 与偏好刷新切片，不替代配对 Watch、Widget timeline 或 Live Activity 真机验收。
- 2026-07-17 fresh timer-admission 定向批次：`StoreScopedTimerCommandCoordinatorTests`、`CoreSystemActionCommandTests`、`CoreWatchCommandTests`、`CoreCompletedTaskSemanticsTests`、task draft/lifecycle 回归共 73/73、0 failed；macOS 自动签名身份为 Team `LT98S43NKA` / `Apple Development: ZEXUAN GAO (PX46M259V3)`。没有创建模拟器；本批 xcresult 已删除，结束时无 owned `xcodebuild`、`xctest` 或受测 App。该证据覆盖普通 Timer 的 fresh parallel/exclusive admission 和 Watch actual outcome events，不替代 Pomodoro、segment writer 或真机 Watch transport 验收。
- 2026-07-17 Pomodoro/Segment fresh-admission 定向批次：Pomodoro start/break-resume/reconcile、active Segment 编辑、Timer Picker、facade 与架构契约共 71/71、0 failed；macOS 自动签名身份为 Team `LT98S43NKA` / `Apple Development: ZEXUAN GAO (PX46M259V3)`。第一次运行由仍传入 `false` 却未写入 canonical preference 的旧测试夹具捕获，夹具改为持久设置后完整重跑通过。没有创建模拟器；两个本批 xcresult 均已删除，结束时无 owned `xcodebuild`、`xctest` 或受测 App。该证据覆盖三个 store-scoped writer 的 fresh setting，不替代真机 Watch/Widget/Live Activity 验收。
- 2026-07-16 正常字号 iPhone UI 批次：iPhone 17 Pro / iOS 27 单设备串行验证 Today 计时选择器、Tasks、Focus、Analytics、Settings 和同步恢复。初次组合运行 3/4，通过的三项为 Analytics hour distribution、Today picker 与跨页面 baseline；同步恢复失败揭示根 Settings 列表串联多个 `confirmationDialog` 且 modifier 位于 compact navigation destination 外，不能计作通过。改为单一枚举 presentation state 并把 modifier 移到 category `Form` 后，两个恢复方向均显示明确红色系统确认，UI 回归 1/1（`/tmp/timetracker-sync-confirmation-rerun3-20260716.xcresult`），只从 `PopoverDismissRegion` 关闭且未执行覆盖动作。随后用 Team `LT98S43NKA` / `Apple Development: ZEXUAN GAO (PX46M259V3)` 执行 Settings 安全合同与同步冲突回归 43/43（`/tmp/timetracker-settings-confirmation-macos-20260716.xcresult`）。截图随 xcresult 保存并已目视；唯一 UDID `6823CFFB-BEB4-409C-A44A-705E65F86D53` 已终止、关闭、删除，设备/booted/xcodebuild/xctest/UI runner 均为 0。`/Applications/timetracker.app` 的既有 macOS 进程启动于本批次之前，未被当作本批次资源终止。截图暴露的 picker 长标题挤压已由后续专门切片解决；浮动 Tab Bar 下方内容仍需继续收敛。本条不是冻结源码全套 UI 通过。
- 2026-07-16 计时选择器布局收口：付费 Apple Development 签名的 command/UI 套件 9/9（`/tmp/timetracker-timer-picker-layout-macos-rerun-20260716.xcresult`）；正常字号 iPhone 17 Pro / iOS 27 UI 1/1（`/tmp/timetracker-timer-picker-ui-color-retry-20260716.xcresult`）。截图确认搜索位于顶部、`Read Apple HIG` 与父路径 `Study` 不被动作挤压、Running/Stop 和 Start 使用独立命令行、任务标题保持主文本色、Stop 图标与文字统一为红色。中间一次 UI 重跑在进入 App 前 launch timeout，不计为通过；两个批次的专用 UDID `472545AF-D9CA-492F-8DE8-1CB54D73EFD9` 与 `CACAFE67-2F93-4A53-97CA-455048502CD3` 均已终止、关闭、删除，设备与 Booted 数为 0。
- 2026-07-16 任务树职责拆分签名批次：task ledger、read-index 与 task UI 合同 65/65（`/tmp/timetracker-task-tree-split-20260716.xcresult`）；精确 structure/Project Map 合同 3/3（`/tmp/timetracker-task-tree-layout3-20260716.xcresult`）。两批都使用 Team `LT98S43NKA` 与 `Apple Development`，证明 624 行聚合实现拆为 7 个 50–121 行文件后行为、UI source consumer、文件存在与 160 行预算保持成立。完整 `CoreSourceLayoutTests` 同时仍有 7 个既存 Analytics 文件预算失败，因此本条只记录 task-tree 切片，不把该 suite 误报为全绿。
- 2026-07-16 durable local-file primitive 审核：第一版测试在祖先查找遇到 Foundation 27 的根目录父路径表示后让测试宿主持续占用 CPU；主 Agent 保存 1 秒 sample（`/tmp/timetracker-durable-file-hang.sample.txt`）后只终止本批次 PID，并把循环改为有界 path-component walk。其后 13/13 初步回归虽通过，但静态并行审查继续发现符号链接隔离逃逸、锁别名、写入/隔离 TOCTOU、发布前保护、回滚诊断、锁文件自替换、目录递归删除和崩溃临时文件泄漏，因此该结果没有提交或冒充完成。最终实现按 lock、公开 API、路径、写入、同步、隔离和修剪拆为 7 个 66–159 行文件，测试也按核心、隔离和锁协作拆分；最终付费签名 macOS 定向回归 23/23、0 新代码 warning（`/tmp/timetracker-durable-file-macos-final-rerun-20260716.xcresult`），精确结构与 Project Map 合同 2/2（`/tmp/timetracker-durable-docs-layout-20260716.xcresult`）。generic iOS 设备 SDK 自动签名重跑成功且 actor-isolation warning 为 0（`/tmp/timetracker-durable-file-ios-signed-rerun-20260716.xcresult`）；主 App、Widget、Live Activity、Watch 均为 `Apple Development: ZEXUAN GAO (PX46M259V3)` / Team `LT98S43NKA`，主 App 保留 development APS、CloudKit 与 App Group，`codesign --verify --deep --strict` 通过。该切片未创建模拟器；结束后无 owned `xcodebuild`、`xctest`、测试宿主或 Booted device。
- 2026-07-16 iPhone Tab Bar 正常字号收口：先用专用 iPhone 17 Pro / iOS 27 设备证明既有 Focus 主动作与 Analytics 最后一项都能完整滚到浮动 Tab Bar 上方，2/2（`/tmp/timetracker-tab-clearance-ui-20260716.xcresult`）；随后为 Phone 根 TabView 启用系统 `.onScrollDown` 收起，并在第二台专用设备复验 2/2（`/tmp/timetracker-tab-minimize-ui-20260716.xcresult`）。截图位于 `/tmp/timetracker-tab-minimize-ui-images-20260716`，确认首屏保留五项导航，下滚 Analytics 后只保留当前标签图标且底部说明不受遮挡。generic iOS 自动签名构建通过（`/tmp/timetracker-tab-minimize-signed-build-20260716.xcresult`），四个嵌入目标均为 Team `LT98S43NKA` / `Apple Development: ZEXUAN GAO (PX46M259V3)`，主 App 保留 development APS、CloudKit、App Group；唯一 build warning 是既有 Widget target 无 AppIntents 依赖时跳过 metadata。专用 UDID `8387436A-C527-4675-B9E9-28AE76A6F378`、`18AB6D99-9C2A-4CE6-BA17-B5C3CF921917` 均已终止、关闭并删除，设备、Booted、runner 与 owned build/test 进程为 0。
- 2026-07-16 Focus 正常字号首屏重排：紧凑宽度缩小卡片/section/page 节奏，任务选择改为标题 + 父级路径，四项计划指标优先同排，并移除与系统 safe area 重复的 112 pt 固定末端空白。付费签名的 macOS 合并定向回归 72/72（`/tmp/timetracker-focus-inbox-llm-macos-20260716.xcresult`）；正常字号 iPhone 17 Pro / iOS 27 Focus UI 1/1（`/tmp/timetracker-focus-layout-ui-20260716.xcresult`），截图位于 `/tmp/timetracker-focus-layout-ui-images-20260716`，确认完整设置卡、Start Focus 和四项摘要均在 Tab Bar 上方。generic iOS 当前合并工作树自动签名构建 0 error/0 warning（`/tmp/timetracker-focus-inbox-llm-ios-signed-20260716.xcresult`）；主 App、Widget、Live Activity、Watch 均为 Team `LT98S43NKA` / `Apple Development: ZEXUAN GAO (PX46M259V3)`，主 App 保留 development APS、CloudKit 与 App Group，严格签名验证通过。专用 UDID `0032D4FB-7AFB-4AB5-9EB1-834A8CD40BCE` 已终止、关闭、删除，设备、Booted、runner 与 owned build/test 进程为 0。本批只验证正常字号和常规路径，不另开极端字号专项。
- 2026-07-16 LLM 模型发现有界解码：网络 `data` 改为逐项解码并只保留与偏好 sanitizer 相同的升序前 256 个唯一、完整、有效 model ID，避免在 2 MiB 总响应预算内仍建立大量临时 Model/Set。付费签名 macOS `LLMSettingsTests` 21/21、0 error/0 warning（`/tmp/timetracker-llm-warning-rerun-20260716.xcresult`），覆盖精确上限、后到更小 ID、重复/空白/控制字符和 ASCII/Unicode byte 边界。当前合并工作树 generic iOS 自动签名构建同样 0 error/0 warning（`/tmp/timetracker-focus-inbox-llm-ios-signed-20260716.xcresult`）；主 App 与三个嵌入目标严格签名通过并保留 Team `LT98S43NKA`、`Apple Development: ZEXUAN GAO (PX46M259V3)`、APS、CloudKit 与 App Group。本批未创建模拟器，结束后无 owned build/test/runner 或 Booted device。
- 2026-07-16 Inbox 刷新纯读投影：逻辑 LWW winner 与 exact revision dismissal 改由 `InboxItemReadModel` 合并，普通 fetch/domain refresh 不再写回 winner。真实 SwiftData 回归确认旧 sibling 的 dismissal 仍会隐藏当前 suggestion，但新 winner 的持久字段不变且 refresh 后 `ModelContext.hasChanges == false`。Inbox identity/apply/persistence/cancellation/store/write-safety 六套付费签名回归 46/46、0 error/0 warning（`/tmp/timetracker-inbox-readmodel-20260716.xcresult`）。当前合并工作树 generic iOS 自动签名构建 0 error/0 warning（`/tmp/timetracker-focus-inbox-llm-ios-signed-20260716.xcresult`），主 App 与所有嵌入目标严格签名通过并保留 Team `LT98S43NKA`、`Apple Development: ZEXUAN GAO (PX46M259V3)` 及 APS/CloudKit/App Group。本批未创建模拟器，结束后无 owned build/test/runner 或 Booted device。
- 2026-07-16 TaskStore 稳定局部顺序：scoped refresh 的 depth/sortOrder/createdAt comparator 增加持久 UUID 终局比较，避免完全相同排序事实受 CloudKit fetch 或字典输入顺序影响。反向输入回归及其余 TaskStore 行为以付费签名运行 6/6、0 error/0 warning（`/tmp/timetracker-task-store-order-20260716.xcresult`）。本批未创建模拟器，测试宿主、xcodebuild、xctest 与 Booted device 审计为空。
- 2026-07-17 Inbox primary command 串行化：`StoreScopedInboxCommandCoordinator` 从只处理 reorder 扩展为 add/toggle/title/delete/discard 的唯一 writer；单项操作以 `itemID + clientMutationID` 在共享 store lock 的 fresh context 内复核，App Intent capture 也改为 container → coordinator → fresh post-commit projection。定向付费签名 macOS `StoreScopedInboxCommandCoordinatorTests`、`CoreSystemActionCommandTests` 与 `CoreSyncConflictTests` 通过；新增 stale item revision 和 facade read-model 收敛回归。初次编译发现 `reorder` 缺少显式 return，已在同一小任务内修正后重跑通过。未创建模拟器；结束时无 owned `xcodebuild`、`xctest` 或受测 App，已有 CoreSimulator 后台服务未触碰。LLM upsert、draft 保存和跨 Inbox/Checklist apply 仍待下一切片锁内复核，未误报为完成。
- 2026-07-17 Inbox suggestion 事务收口：新增 `StoreScopedInboxSuggestionCommandCoordinator`，把手工 draft、LLM completion upsert 与跨 Inbox/Checklist apply 纳入同一 fresh store transaction。apply 在锁内重验 logical identity、canonical ready suggestion、trackable task、visible Checklist 和 ancestor IDs；LLM 只依赖 title/revision/dismissal/task 事实，纯 reorder 不会误丢有效回应。第一次定向回归发现未配置 persistence 的纯内存 facade 测试因前置任务校验被延后而改报“store 未配置”；保留廉价 UI 预检、但锁内重新校验仍为写入权威后，完整 Inbox/LLM/identity/apply/persistence/completed-task/source-layout 付费签名批次通过。generic iOS Debug 自动签名构建也通过，主 App、Widget、Live Activity 和 Watch 均为 Team `LT98S43NKA` / `Apple Development: ZEXUAN GAO (PX46M259V3)`。未创建模拟器；结束后无 owned `xcodebuild`、`xctest` 或受测 App，已有 CoreSimulator 后台服务未触碰。
- 2026-07-16 计时准入纯策略底座：新增 canonical active snapshot、start/stop plan 与稳定 policy，覆盖同任务 survivor/duplicate cleanup、Pomodoro 类 `replaceAll`、exclusive/parallel、精确 segment/task/current stop 和 UUID tie-break。付费签名 macOS 定向测试 12/12、0 error/0 warning（`/tmp/timetracker-timer-admission-policy-20260716.xcresult`）；本批未创建模拟器，测试宿主、xcodebuild、xctest 与 Booted device 审计为空。该提交尚未把生产 UI/SystemAction/Watch/AppIntent/Pomodoro writer 接入跨进程协调器，因此明确不代表计时竞态已经修复。
- 2026-07-16 任务编辑器符号子流程收口：iOS 从内层 sheet + 独立 NavigationStack + 伪 Done 改为外层编辑器 NavigationStack 内的 push，macOS 保持 popover；标题键盘支持 Done/滚动收起，首次自动聚焦不会在 Back 后重跑。付费签名 macOS `TaskUIContractTests` 34/34、0 error/0 warning（`/tmp/timetracker-symbol-navigation-final-macos-20260716.xcresult`）。正常字号 iPhone 17 Pro / iOS 27 最终 UI 回归 1/1、0 warning（`/tmp/timetracker-symbol-navigation-ui-focus-pass-20260716.xcresult`），截图导出到 `/tmp/timetracker-symbol-navigation-ui-focus-export-20260716`；确认进入符号页未增加 sheet、选择 `calendar` 后 Back 保留 `Navigation draft`，并且外层页面不重新弹键盘。generic iOS 自动签名构建 0 error/0 warning（`/tmp/timetracker-symbol-navigation-final-ios-20260716.xcresult`），四个产品 bundle 通过 `codesign --verify --deep --strict`，均为 Team `LT98S43NKA` / `Apple Development: ZEXUAN GAO (PX46M259V3)`；主 App 最终签名包含 development APS、CloudKit `iCloud.me.mezorewww.timetracker` 与 App Group `group.me.mezorewww.timetracker`。发现阶段设备 `D9481DBF-2C08-493C-980B-9A7C96BDE2D1` 和最终设备 `10C2F25D-E4CE-4B37-BC80-7640936B051B` 均已终止、关闭、删除；失败路径遗留的三个本批 `simctl diagnose` 进程也被显式终止，最终 device/Booted/xcodebuild/xctest/UI runner/diagnose 审计均为 0。本批仅验证正常字号常规流程，没有新增辅助功能专项。
- 2026-07-16 Quick Start 任务身份统一：iPhone 行、iPad/macOS tile 与编辑器改为 canonical 任务 symbol + 标题 + 不含自身的父级路径，根任务不重复标题；播放/停止 glyph 独立表达动作。付费签名 macOS identity/Home 定向套件 22/22、0 error/0 warning（`/tmp/timetracker-quickstart-macos-tests-20260716.xcresult`）。最终正常字号 iPhone 17 Pro / iOS 27 UI 1/1、0 warning（`/tmp/timetracker-quickstart-iphone-ui-framed-20260716.xcresult`），截图导出到 `/tmp/timetracker-quickstart-iphone-images-framed-20260716`；目视确认 `Time Tracker App` 根任务无副标题，`Design System` 显示父路径，任务 icon 与右侧 play/红色 stop 分离，整段完整位于导航栏和收起后的系统 Tab Bar 之间，编辑器使用相同层级。首轮 UI 虽通过但截图被 Tab Bar 遮挡、一次冷启动 timeout、一次取景过度滚动均不作为最终视觉证据；macOS UI runner 两次因系统认证正在运行而在初始化前取消，同样不计通过。合并工作树 generic iOS 自动签名构建 0 error/0 warning（`/tmp/timetracker-identity-timer-ios-signed-20260716.xcresult`）；主 App、Widget、Live Activity、Watch 均通过 `codesign --verify --deep --strict`，为 Team `LT98S43NKA` / `Apple Development: ZEXUAN GAO (PX46M259V3)`，主 App 保留 development APS、CloudKit 和 App Group。四台专用设备 `A2AE3E28-16C2-48C5-8F81-CB4AB8879482`、`C0A7301A-AE5B-4411-95A6-B57B8695D789`、`2E9C258C-039D-44E0-96B4-3571A4CF9C58`、`6EDA572F-4EB4-45F6-A8AB-726E593444AC` 均已终止、关闭、删除；最终无 Booted device、owned xcodebuild/xctest/UI runner 或 diagnose 进程。本批只验证正常字号常规路径，没有新增辅助功能专项。
- 2026-07-16 store-scoped 计时事务底座：新增 persistent/in-memory `TimerStoreScope`、复用 durable `PathFileLock` 的跨进程锁，以及“锁内新建 context + 单次 atomic mutation”的 transaction。付费签名 macOS 定向测试 6/6、0 error/0 warning（`/tmp/timetracker-timer-lock-foundation-20260716.xcresult`），覆盖路径别名、同/异 store 并发、抛错释放、fresh context、提交与回滚。合并工作树 generic iOS 自动签名构建与四个产品 bundle 的严格签名/entitlement 审计见 `/tmp/timetracker-identity-timer-ios-signed-20260716.xcresult`；Team `LT98S43NKA`、付费 Apple Development、development APS、CloudKit 和 App Group 均保留。本批未创建模拟器，结束后无 owned xcodebuild、xctest、runner、diagnose 或 Booted device。底座尚未接入 UI/SystemAction/Watch/AppIntent/Pomodoro writer，因此明确不代表计时竞态已经修复。
- 2026-07-16 任务导航单一状态源：三平台统一由 `TasksNavigationView` 持有系统 `NavigationStack`，`TasksRoute?` 只表达页面，`selectedTaskID` 继续表达业务选择；移除 `TasksView` 本地详情 ID、桌面根替换、自绘 Back 和手动 dismiss。返回保留搜索/展开，删除失败留在详情，删除父树/外部删除/维护刷新只在成功或失效时清 route，sidebar selection 由 route 派生。付费签名 macOS route/deep-link/lifecycle/UI-contract 定向回归 75/75、0 warning（`/tmp/timetracker-tasks-route-tests-rerun-20260716.xcresult`）；第一次编译因新增测试中的 optional chaining 错误失败且 0 tests，修正后才取得该通过结果。正常字号 iPhone 17 Pro / iOS 27 UI 1/1（`/tmp/timetracker-tasks-route-iphone-ui-20260716.xcresult`），截图导出到 `/tmp/timetracker-tasks-route-iphone-images-20260716`，目视确认系统 Back 以及返回后 `Study` 分支仍展开。iPad Pro 13-inch (M5) / iOS 27 在进入 App 前 launch timeout，自动重启执行 0 tests，随后 Xcode 卡在日志收尾；该 owned build 与 `simctl diagnose` 被显式终止，损坏的临时 result bundle 不计 UI 通过，也不继续消耗资源重试。generic iOS 设备 SDK 自动签名构建 0 error/0 warning（`/tmp/timetracker-tasks-route-ios-signed-20260716.xcresult`）；主 App、Widget、Live Activity、Watch 均通过 `codesign --verify --deep --strict`，Team `LT98S43NKA` / `Apple Development: ZEXUAN GAO (PX46M259V3)` 保持，主 App 保留 development APS、CloudKit 与 App Group。专用 UDID `71F565DC-A66C-4E53-96FF-A8B66362B91E`、`D8A1C058-3AA3-4E10-BE7E-16DADE9C2A12` 均已终止、关闭、删除；最终无 Booted device、owned xcodebuild/xctest/UI runner/diagnose、Simulator 或 Problem Reporter。本批只覆盖正常字号常规路径，不安排额外 Accessibility 专项。
- 2026-07-16 scene-owned typed presentation：把共享 facade 中六组 sheet draft/bool 与 feature-local Quick Start/LLM/Today/task picker sheets 收敛为每个 scene 一个 `AppPresentationRouter` + `AppPresentationHost.sheet(item:)`。七类 typed content 由 matching ID 仲裁，忙时不覆盖当前草稿；任务选择器→新任务改为原子 replace，不再 dismiss 后 `Task.yield()`。主窗口与 macOS Settings 共享 Store 但各自持有 router；Settings 删除错位的“手动补录”，Mac 命令使用 focused router 并在 busy 时禁用。dead Inbox editor UI 删除，apply/discard 保留。deep link 增加 handled/deferred/rejected，modal/nav 动作先取得 slot、busy 时进入有界 queue，direct start/stop 不被无关 sheet 阻塞。首次付费签名 macOS build 因跨文件 `private` environment 属性编译失败（`/tmp/timetracker-scene-presentation-macos-build-20260716.xcresult`），修正后 0 error/0 warning（`/tmp/timetracker-scene-presentation-macos-build-rerun-20260716.xcresult`）；首次测试因新增 `#expect` 内 mutating call 编译失败、执行 0 tests（`/tmp/timetracker-scene-presentation-tests-20260716.xcresult`），第二次执行 96 项但两项 source contract 失败（`/tmp/timetracker-scene-presentation-tests-rerun-20260716.xcresult`），最终定向套件 96/96、0 skip/runtime warning（`/tmp/timetracker-scene-presentation-tests-final-20260716.xcresult`）。正常字号 iPhone 17 Pro / iOS 27 两条常规 UI 流程 2/2（`/tmp/timetracker-scene-presentation-iphone-ui-20260716.xcresult`），截图位于 `/tmp/timetracker-scene-presentation-iphone-images-20260716`；任务编辑截图有系统一次性键盘教学浮层，Today 与任务选择器截图确认单一 sheet 层级。generic iOS 自动签名构建 0 error/0 warning（`/tmp/timetracker-scene-presentation-ios-signed-20260716.xcresult`）；四个产品 bundle 严格签名通过，保持 Team `LT98S43NKA`、`Apple Development: ZEXUAN GAO (PX46M259V3)`、development APS、CloudKit 和 App Group。专用 UDID `4ECB7632-880B-45B8-9E04-7045B511B895` 已终止、关闭、删除，最终无 Booted device、owned xcodebuild/xctest/UI runner、Simulator 或 Problem Reporter。本批仅检查正常字号常规路径；store error/sync-conflict alert 的 scene 归属明确留给下一独立任务。
- 2026-07-16 任务行删除确认单一 owner：Tasks 与 Sidebar 的 context menu 和 swipe 统一把 `requestDelete` 交给 row；共享 `TaskRowSwipeActions` 删除第二份 Bool 与第二个 `confirmationDialog`，确认继续使用显式 task UUID。菜单的 “Soft Delete/软删除” 改为与 swipe/确认一致的 Delete/删除，三语死键删除。付费签名 macOS `TaskUIContractTests` 最终 34/34、0 skip/runtime warning（`/tmp/timetracker-delete-confirmation-tests-final-20260716.xcresult`）；首次 34/34 发生在文案统一前，只作中间编译证据，最终结果覆盖完整改动。本批未创建模拟器，最终无 owned xcodebuild/xctest/runner 或 Booted device；没有安排 Accessibility 专项。
- 2026-07-16 同步冲突确认 CAS：Content 与 Settings 的确认 payload 捕获用户实际看到的 optional conflict ID；service 在跨进程 state lock 内、任何 epoch/model/reset/state 写入前精确比较。pending local/cloud resolution branch fingerprint 变化时旋转 token并返回更新后 prompt；expected nil 期间出现冲突、stale ID 或旧摘要 token 全部返回 `conflictChanged` 且零副作用。结果区分 `appliedImmediately`/`queuedForNextLaunch`，Store 只把异常映射为 `failed`。验证中第一次 build 因 resolution 文件缺 `Foundation` 无法识别 UUID（`/tmp/timetracker-conflict-identity-tests-20260716.xcresult`），第二次因 private locked helper 仍是 nonoptional UUID 编译失败（`/tmp/timetracker-conflict-identity-tests-rerun-20260716.xcresult`），第三次 production 编译通过但 Agent 新测试的 throwing optional 宏与 actor 标注错误导致 0 tests（`/tmp/timetracker-conflict-identity-tests-rerun2-20260716.xcresult`）；第四次执行 47 项，其中 45 通过、两项旧 token/source-location 预期失败（`/tmp/timetracker-conflict-identity-tests-rerun3-20260716.xcresult`）。修正测试语义后最终付费签名 macOS 47/47、0 skip/runtime warning（`/tmp/timetracker-conflict-identity-tests-rerun4-20260716.xcresult`）。generic iOS 自动签名构建 0 error/0 warning（`/tmp/timetracker-conflict-identity-ios-signed-20260716.xcresult`），四个产品 bundle 严格签名通过并保持 Team `LT98S43NKA` / `Apple Development: ZEXUAN GAO (PX46M259V3)`；主 App 保留 development APS、CloudKit 和 App Group。本批未创建模拟器，最终无 Booted device、owned xcodebuild/xctest/runner；没有安排 Accessibility 专项。
- 2026-07-16 冲突 prompt 显式失败：`SyncConflictService.prompt()` 不再用 `try?` 把损坏/超限/IO 错误伪装成无冲突；前台与 observer 沿 throwing 边界报告，Watch 已提交命令则保留 terminal 结果并把 prompt 失败归入 post-commit refresh。付费签名 macOS 冲突、identity 与 Watch 定向套件 74/74、0 skip/runtime warning（`/tmp/timetracker-throwing-conflict-prompt-tests-rerun-20260716.xcresult`），新增损坏 state 读取会抛错并隔离的覆盖；签名为 Team `LT98S43NKA` / `Apple Development: ZEXUAN GAO (PX46M259V3)`。本批未创建 simulator；结束后终止 owned app 与 TestManager，最终无 Booted device、owned xcodebuild/xctest/runner。
- 2026-07-16 CloudKit 启动恢复门控：queued upload/download reset 改为 typed completed/deferred/failed；缺失或不可读的受保护上传快照、store 删除失败、同步状态删除失败都保留 pending 并阻止本次云容器创建。只有 CloudKit container 成功后才能消费不可伪造 completion token 清标记；门控位于 demo 与用户禁用分支之后。相关同步回归 49/49（`/tmp/timetracker-cloud-recovery-gate-tests-20260716.xcresult`），最终 MainActor 删除闭包回归 6/6（`/tmp/timetracker-cloud-recovery-isolation-tests-rerun-20260716.xcresult`）。generic iOS 最终 0 error/0 warning（`/tmp/timetracker-sync-conflict-notice-ios-build-final-rerun2-20260716.xcresult`），主 App、Widget、Live Activity、Watch 严格签名为 Team `LT98S43NKA` / `Apple Development: ZEXUAN GAO (PX46M259V3)`，主 App 保留 development APS、CloudKit 与 App Group。本切片未创建模拟器。
- 2026-07-16 同步冲突非阻断导航：删除根 scene 自动破坏性 confirmation，改为 scene-local `SyncConflictNotice`；iOS 材质卡固定在浮动 Tab Bar 上方，macOS 使用顶部 inset，Review 只导航 Settings。Settings 先显示本机/iCloud 摘要，再提供两个红色覆盖方向；忽略不清 pending，token 变化才再次提示。付费签名 presentation 契约最终 11/11（`/tmp/timetracker-sync-conflict-notice-macos-tests-final-20260716.xcresult`）；正常字号 iPhone 17 Pro / iOS 27 常规流程 1/1（`/tmp/timetracker-sync-conflict-notice-iphone-tests-card-final-20260716.xcresult`），最终截图位于 `/tmp/timetracker-sync-conflict-notice-iphone-attachments-20260716-card-success`。发现期依次暴露 UI-test observer race、父标识覆盖子按钮、摘要需滚动和底部 inset 与浮动 Tab Bar 重叠；这些中间失败/视觉拒绝均不计最终通过。五台 owned UDID `8AF26D1B-156E-4534-8FED-824855A2D36B`、`721716EE-16D8-4FC6-BB3A-55E0C233C6E9`、`BBB7652C-6B36-4A35-A423-3CA272A413F2`、`AB2183F1-8C1B-4103-B3AC-91B978C76B1B`、`EC395A18-E84B-4262-9013-ED492D164C06` 均已终止、关闭并删除；本批只验证正常字号常规路径，没有安排 Accessibility 专项。
- 2026-07-17 scene-owned feedback：主窗口与 macOS Settings 各自持有 FIFO `AppSceneFeedbackRouter`/host，dismiss 绑定精确 feedback UUID；JSON export、数据库清理与同步恢复失败只进入发起 scene，成功/排队/冲突变化就地显示。队列核心与 presentation 11/11（`/tmp/timetracker-scene-feedback-core-tests-20260717.xcresult`）；scene/JSON/清理契约两组各 34/34（`/tmp/timetracker-settings-scene-feedback-tests-20260717-final.xcresult`、`/tmp/timetracker-settings-scene-feedback-taskui-tests-20260717.xcresult`）；sync conflict/stale token/recovery 55/55（`/tmp/timetracker-sync-recovery-scene-feedback-tests-20260717.xcresult`）。全部使用 Team `LT98S43NKA` / `Apple Development: ZEXUAN GAO (PX46M259V3)`，0 skip/runtime warning；未创建模拟器，每批资源审计为空。
- 2026-07-17 typed Cloud activity：`SyncActivityOutcome` 区分 import/export/setup 的成功与失败，remote-store 信号不生成成功，本机 refresh/冲突/checkpoint 后处理失败覆盖事件成功；账户检查独立，未来时间或超过 120 秒不显示最近绿色状态。同步活动、账户、冲突与 Settings 契约 76/76（`/tmp/timetracker-sync-activity-tests-20260717-b.xcresult`），使用同一付费签名身份，0 failed/skip/runtime warning；未创建模拟器，资源审计为空。
- 2026-07-17 Quick Start 稳定语义：未运行任务开始/切换，运行任务显示 Running 并打开 canonical 任务详情，整行不再按隐藏状态变成停止。签名 macOS 相关 command/UI 套件 60/60（`/tmp/timetracker-quick-start-semantics-tests-20260717.xcresult`）；正常字号 iPhone 17 Pro / iOS 27 精确回归 1/1（`/tmp/timetracker-quick-start-running-detail-ui-20260717-b.xcresult`）并目视检查详情页 Running 截图。唯一 owned UDID `9A2172FF-BC8A-4EBC-B24B-6B7EFF70D606` 已终止、关闭并删除，最终无 Booted device、runner、`xcodebuild`、`xctest`、Simulator、Problem Reporter 或 trace 残留。
- 2026-07-17 系统表面精确停止：Stop Timer App Intent、Widget 和 Live Activity 固化 segment UUID；旧无目标入口只在唯一活动计时时兼容，并行时拒绝，过期目标不回退。签名 macOS command/deep-link/系统表面套件 40/40（`/tmp/timetracker-targeted-stop-tests-20260717.xcresult`）；generic iOS Debug 自动签名构建通过（`/tmp/timetracker-targeted-stop-ios-build-20260717.xcresult`），主 App、Widget、Live Activity、Watch 均使用 Team `LT98S43NKA` / `Apple Development: ZEXUAN GAO (PX46M259V3)` 的 provisioning profile 完成嵌入验证。本批未创建模拟器，最终资源审计为空。
- 2026-07-17 Checklist store-scoped 事务域：quick add、显式 completion 与 reorder 改为锁内 fresh context，并以 item/order mutation baseline 拒绝旧 scene；新增同时验证 canonical task、复用 editor 持久化限制并返回 fresh ancestor chain。付费签名 macOS 两批定向回归合计 55/55，覆盖 coordinator 7 项、原 command handler、forecast/偏好 checklist、三语本地化、source map 与 task draft 跨场景交互；首次编译只因 Facade 缺少显式 `SwiftData` import 失败，补齐后才取得通过结果。本批未创建模拟器，两个临时 DerivedData 与 owned `xcodebuild`/`xctest`/App 进程均已清理。
- 2026-07-17 Checklist AI visual stale-write 防护：异步建议请求固化 item/title/visual revision；completion 改在 shared lock + fresh context 内重新验证 trackable task、item mutation、标题、logical visual revision 与 user edit，再调用 writer。另一个 scene 的手动图标/颜色修改、完成、删除、标题或任务生命周期变化都会无副作用地丢弃迟到结果，只刷新当前 read model。付费自动签名 macOS `StoreScopedChecklistCommandCoordinatorTests`、`CoreInboxStoreTests`、`CoreSourceLayoutTests` 合计 55/55、0 failed/skipped/runtime warning；首轮只暴露 Xcode 27 的显式 `SwiftData` member-import 要求，补齐 import 后重跑通过。本批没有创建或操作模拟器；临时 DerivedData、xcresult 与 owned build/test/App 进程在提交前清理。
- 2026-07-17 多 scene 本地提交收敛：把仅 System Action 使用的进程内通知泛化为 `StoreMutationBroadcaster`；普通 durable commit 在本 scene refresh/snapshot 后按 source store 广播精确 events，sibling 只做 read-model refresh，任务/ledger event 额外校正失效 selection/route，不触发 LLM、snapshot 或循环广播。付费自动签名 macOS `CoreSystemActionCommandTests`、`CoreTasksRouteTests`、`CoreSourceLayoutTests` 合计 57/57、0 failed/skipped/runtime warning；新回归覆盖跨 scene Inbox 立即出现且自动建议保持 idle、删除任务后的 selection/detail route 收敛，以及无 source 的 System Action 全 scene 刷新。本批没有创建或操作模拟器；临时 DerivedData、xcresult 与 owned build/test/App 进程在提交前清理。跨进程仍明确由 durable snapshot、persistent store/CloudKit 与 system-surface paths 收敛。
- 2026-07-17 Analytics 确定性选择：Task breakdown、Peak Hour 与删除任务 title fallback 统一完整决胜规则，Task breakdown 从聚合文件拆为独立 owner。首轮付费签名 macOS Analytics timeline/store + 新 permutation + source-layout 回归 59/59；该轮发现测试直接比较 actor-isolated `Equatable` 的两条 Swift 6 warning，删除不必要 conformance 后最终新套件/source 契约 5/5、0 warning。本批未创建模拟器，临时 DerivedData 与 owned build/test 进程均已清理。`analyticsStoreFilesStaySplitByResponsibility` 仍因其它既存超预算文件未全绿，本条不宣称完整 source-layout 通过。
- 2026-07-17 Category store-scoped 事务域：create/update/delete 与 task-draft assignment 共用锁内 fresh context，editor/delete baseline 拒绝旧 scene，删除同事务清理 assignments，missing repository mutation 不再静默成功；分类 draft 模型拆出后通用 `EditorDraftModels.swift` 回到 230 行预算内。付费签名 macOS 分类、Task Draft、presentation、本地化与两项 source-layout 定向批次全部通过；本批未创建模拟器，临时 DerivedData 与 owned build/test 进程在结束时清理。最终精确 coordinator 重跑见同提交验证记录，不代表冻结树完整 suite。
- 2026-07-17 Task Editor 层级职责拆分：信息 section 从 209 行降为 138 行，parent/category/hints 独立为 129 行轻量投影；父项改用 canonical full path，继承分类仅图标着色，active subtree 查询在同一 body 只执行一次。首次签名回归中全部行为/Task UI 契约通过，但 source-layout 暴露既有 `SymbolPickerViews.swift` 231>230；没有删空行凑数，而是把共享 `SymbolColorPickerRow` 拆出，最终完整 Task UI + editor/source map 契约全部通过。本批未创建模拟器，临时 DerivedData 与 owned 进程均清理；没有安排 Accessibility 专项。
- 2026-07-17 Task Editor stale 恢复：保存边界增加 typed saved/stale/failed，stale 在当前 sheet 提供显式 Reload Latest/Keep Draft；重载同步替换 mutation/discard baseline 与父候选，普通 Bool API 保持兼容。付费签名 macOS 新恢复、Task Draft、完整 Task UI、presentation、本地化和 Facade 预算批次全部通过且无 warning；未创建模拟器，临时 DerivedData 与 owned build/test/App 进程已清理。本批只验证正常编辑流程，没有安排 Accessibility 专项或自动三方 merge。
- 2026-07-17 同步恢复渐进披露：无冲突时恢复区默认只显示“打开恢复选项”，主动展开后才出现两个红色覆盖方向；真实冲突继续直接按本机摘要、iCloud 摘要、方向动作的顺序展示。付费签名 macOS Settings 安全、presentation 与三语 parity 合同 26/26，UI test target 的 `build-for-testing` 也通过；generic iOS 设备 SDK 自动签名构建 0 error/0 warning，主 App、Widget、Live Activity、Watch 均为 Team `LT98S43NKA` / `Apple Development: ZEXUAN GAO (PX46M259V3)`，主 App 保留 development APS、CloudKit 与 App Group。一次正常字号 iPhone 17 Pro / iOS 27 单测试尝试在 runner 启动前卡于 Xcode test-log 收尾并由主 Agent 中止，不计 UI 通过，也没有可用截图；唯一 owned UDID `DBDC337D-F6E7-4768-AD76-CAD01219978D`、设备目录、临时 DerivedData、卡住的 `xcodebuild` 与 `simctl diagnose` 均已清理，损坏的 382 MiB result bundle 已删除，最终 owned 设备/进程、Simulator 与 Problem Reporter 为 0。
- 2026-07-17 同步冲突提示 shell ownership：删除 iOS 根层固定 `84 pt` bottom overlay；第一版让 iPhone 使用 `tabViewBottomAccessory`，静态 HIG 复核发现完整两行通知并非会随 Tab Bar 收缩的紧凑持久控制，因此在相邻提交纠偏为三平台各自 shell 的顶部 safe-area inset。主持久化不可写时不在恢复页叠加冲突提示。付费签名 macOS presentation、Home/iPad shell 与根文件预算定向合同全部通过；generic iOS 设备 SDK 自动签名构建 0 error/0 warning，主 App 与三个嵌入目标均为 Team `LT98S43NKA` / `Apple Development: ZEXUAN GAO (PX46M259V3)`，主 App 保留 development APS、CloudKit 与 App Group。本切片没有再启动模拟器，视觉复验明确留给后续正常字号 UI 批次；两个临时 DerivedData 与 owned build/test/App 进程均已清理。
- 2026-07-17 Inbox store-scoped 重排：拖拽携带完整 open logical winner 的 mutation map 与确定性原顺序，在共享 store lock 内用 fresh context 复核；并发新增、完成、编辑、删除、其它重排或缺失 revision 的 order-only 变化都会拒绝旧操作并刷新 scene。首轮在执行测试前因 Facade 缺少显式 `SwiftData` import 编译失败，补齐后较宽 Inbox identity/持久化/UI/本地化批次 35/35；最终 coordinator/mutation/UI/本地化批次 23/23、完整 command handler 17/17，均使用付费 macOS 签名。Source-map 文件存在、119 行预算与三份文档入口另以直接检查通过。本批未创建模拟器，所有临时 DerivedData 与 owned build/test/App 进程均已清理；add/toggle/title/delete/suggestion writer 仍明确属于后续事务切片。
- 2026-07-17 Analytics 跨日本地日 identity：landing/category request 与 full/task cache 统一使用完整 interval、current local-day 和 optional live-minute 组成的 `AnalyticsEvaluationCacheKey`；静态当前 week/month 在午夜强制 miss，completed history 保持稳定。缓存职责从 209 行主文件拆为 99 行 generation owner 与 148 行 `AnalyticsStore+Caching.swift`。首轮 Analytics 批次中新跨日测试全部通过，但一项低层历史 cache 测试未传显式 key，另有一项与本改动无关的旧 Ledger source contract 仍要求 Facade 直接查 index；前者修正，后者没有混入本提交。最终 RefreshPlan、完整 AnalyticsStore 与 Task/Timeline 套件 64/64；generic iOS 设备 SDK 自动签名构建 0 error/0 warning，主 App 为 Team `LT98S43NKA` / `Apple Development: ZEXUAN GAO (PX46M259V3)` 并保留 development APS、CloudKit 与 App Group。本批未创建模拟器，所有临时 DerivedData 与 owned build/test/App 进程均已清理。
- 2026-07-17 Segment architecture contract 校正：旧源码断言仍要求 Ledger facade 直接调用 `ledgerDomainStore.segment(for:)`，与已经完成的 store-scoped segment coordinator 架构相反。合同改为验证 Facade 委托 coordinator、锁内 fresh context、repository 精确 ID 查询且两层都不扫描 `allSegments.first`；本提交不改生产行为。付费签名的完整 `CoreArchitectureBehaviorTests` 8/8 通过，临时 DerivedData 与 owned build/test/App 进程已清理。
- 2026-07-17 Analytics cache model retention：删除 full/task snapshot 中无生产读取者的 `[TimeSegment]` 字段，生成管线仍使用 scoped segment 输入，但缓存只持有公开 read models；重复-row 测试改为核对 timeline 等输出，不检查内部模型数组。付费签名的完整 AnalyticsStore + Timeline 回归 57/57 通过；本批未创建模拟器，临时 DerivedData 与 owned build/test/App 进程已清理。
- 2026-07-17 Manual Time 完整高度：iOS 手动补录删除 `.medium` detent，与 Segment Editor 一致只保留 `.large`，避免半高表单无法直接完成任务/时间/验证/备注流程。付费签名的 Task UI + App presentation 合同 48/48 通过；本批未创建模拟器，视觉复验并入后续正常字号 UI 批次，临时 DerivedData 与 owned build/test/App 进程已清理。
- 2026-07-17 Analytics comparison 中立语义：没有用户目标、预算或期望方向时，记录时长的增加和减少都只作为 neutral 事实呈现，不再把“记录更少”擅自标为绿色 positive。付费自动签名的完整 `CoreAnalyticsStoreTests` 37/37 通过，新增回归同时覆盖正差与负差；本批未创建模拟器，临时 DerivedData 与 owned build/test/App 进程已清理。
- 2026-07-17 Task Detail analytics 有界读取：活动任务详情的分钟级刷新从“按任务分支读取全部历史”改为 day index 上的当前/上一周期范围，并通过每任务最近 8 条索引独立生成 recent records；索引按 startedAt/UUID 确定性排序，删除后会补齐。付费签名的 Ledger、Analytics、Timeline、架构与三项 source-layout 合并回归 79/79 通过；首轮行为通过但暴露默认 MainActor 下的静态 helper 隔离 warning，不计作最终零 warning 证据，显式 `nonisolated` 后完整重跑 79/79 且无新 Swift warning。本批未创建模拟器，两个临时 DerivedData 与 owned build/test/App 进程均已清理。
- 2026-07-17 Task analytics 日期/任务索引交叉：新增 task-scoped interval query，先比较 task bucket 计数与日期候选上界，选择较小一侧后再做精确 overlap，不再物化所选周期内全 App 的无关模型。付费签名的 Ledger、Analytics、Timeline、架构与三项 source-layout 回归 80/80 通过，覆盖稀疏任务与深历史任务的两条 planner 路径；无新 Swift warning。本批未创建 simulator，临时 DerivedData 与 owned build/test/App 进程已清理。
- 2026-07-17 Local fallback commit/snapshot 崩溃窗口：Cloud-enabled factory 用同一外层 store-scoped lock 覆盖重开本地库、fresh 全域 capture、state/mirror 保存、恢复 marker 与 destructive reset，防止另一进程在补快照和删库之间插入提交；pending download 保留用户选择并跳过 recapture，任一保护失败都不删本地库。第一版 preflight 虽通过 21/21，却在 snapshot 与 reset 之间释放锁，经主 Agent 复核拒绝提交；改为外层锁后首轮 20/21，唯一失败是旧 source contract，不计最终通过。最终付费签名 macOS Gate、SyncConflict、StateWrite、StoreSerialization、ResolutionIdentity、SnapshotPreflight 六套 102/102，真实磁盘 fixture 验证漏记 commit 被补抓、SQLite 删除前临时 container 已释放、竞争 writer 直到 reset 后才入锁；无源码/runtime warning。本批未启动 simulator，所有临时 DerivedData 与 owned build/test/App 进程已清理。剩余风险是部分 legacy Inbox/App Intent writer 尚未进入共享锁域。
- 2026-07-17 Task Detail 核心先渲染：移除整页 `TimelineView`/loading replacement；身份、计时动作、清单、预测与备注立即显示，分析快照以 request-keyed task 单独加载，旧 range/revision 快照不会短暂混入。刷新只在 active scene 由 `AnalyticsRefreshPlan` 调度到真实分钟或本地日边界，回到前台、时钟/时区变化重新取样。付费签名 macOS Core architecture、Task UI contract 与 refresh-plan 定向回归 48/48 通过；generic iOS Debug 自动签名构建通过，主 App/Widget/Live Activity/Watch 皆为 Team `LT98S43NKA` / `Apple Development: ZEXUAN GAO (PX46M259V3)`，主 App 保留 development APS、CloudKit 与 App Group。iPhone 17 Pro / iOS 27 默认字号“运行中 Quick Start → Task Detail”UI 用例 1/1 通过，截图目视确认 Stop/Add Time/Forecast 都在首屏。两轮专用 UDID、result bundle、截图、DerivedData、runner 与受测 App 均已终止/删除；没有保留模拟器。
- 2026-07-17 Analytics source ownership：将 comparison window（含 DST/短月 progress 对齐）、raw metrics、comparison decision、insight narrative、cache-aware daily snapshot assembly 和 facade UI projections 拆为明确 owner，恢复 Analytics domain/facade 的行数预算，不改 public API、cache key 或统计语义。付费自动签名的 macOS `CoreSourceLayoutTests`、`CoreAnalyticsStoreTests`、`AnalyticsTimelineTests` 定向回归通过；本批未启动 simulator，临时 DerivedData 与 owned build/test/App 进程均在退出时清理。
- 2026-07-17 Task Detail hierarchy：将任务标题收敛到系统 navigation title，identity row 改为父级/root context + status，避免首屏重复标题；normal-size iPhone list 增加 16 pt scroll-content 底部余量，使末项不贴入系统 Tab Bar glass。付费自动签名 macOS Task UI/Core architecture/refresh/source-layout 定向回归与 generic iOS Debug 自动签名构建均通过。iPhone 17 Pro / iOS 27 本轮 UI TestManager 在测试 host 构建后无进展，直接 seeded launch 只显示空白系统画布；两者不计 UI 通过，不重复创建更多设备。专属 UDID `4EF8AB4E-F59D-4D20-9418-29E6210C592C`、`1CF33393-99F7-4899-B604-8AAA0EB91357`，受测 App、DerivedData、result 与截图目录均已删除，最终无 owned Booted device/runner。
- 2026-07-17 Quick Start 导航与计时动作拆分：任务身份在 Phone、iPad 与 macOS 始终打开 canonical Task Detail；独立的 44 pt 系统 bordered `Start`/`Switch`/`Stop` 负责写入，Stop 固定当前 segment，避免状态改变整行含义或嵌套按钮。付费自动签名 macOS Home/Task/Timer Picker 合并定向回归 59/59、0 failed/skipped；iPhone 17 Pro / iOS 27 正常字号 `Start → Running → 滚动至 Tab Bar 上方的 Stop → Edit Quick Start` UI 用例 1/1（`/tmp/timetracker-quick-start-separate-actions-ios-final-20260717.xcresult`）。另一个“运行中点任务身份进入详情而不停止”的 UI worker 已成功结束，但 Xcode Beta 在完成测试后卡于 `simctl diagnose`，主 Agent 为释放资源终止收尾采集；未形成可读取 result bundle，故不计作最终 UI 通过。该批专属 UDID `E27A2A8E-CCBF-430F-BFBC-FA86F563268C`、受测 App、两轮 result/DerivedData 与截图目录均已删除，最终无本批 owned simulator、runner、`xcodebuild`、`xctest` 或 trace。
- 2026-07-17 App Intent scene 收敛：Start/Stop/Capture 的 post-commit snapshot、system surface refresh 与本机 event broadcast 合并为单一 owner；已配置 scene 只按实际 events 读刷新，不重复 snapshot/surface，也不自动启动 LLM。付费自动签名 macOS `CoreSystemActionCommandTests`、Timer Picker command 与 system-surface contract 合计 23/23、0 failed/skipped/runtime warning（`/tmp/timetracker-system-action-scene-refresh-20260717.xcresult`）；其中双 scene 的独立 `ModelContext` 回归证明外部 Start/Stop 同步刷新两个 active timer cache。没有启动模拟器；本批 DerivedData、result 和 owned build/test process 均已删除，最终无本批 owned `xcodebuild`、`xctest`、受测 App 或 trace。
- 2026-07-17 SyncConflict durable state：权威 state、recovery mirror、默认清除与损坏隔离接入 `DurableLocalFile`，保留原有 cross-process state lock；production root 让 `TimeTrackerSync` directory 的发布也获得父目录 sync，state/mirror 两个文件仍明确不是 ACID transaction。新增 fault injection 覆盖 state publish 前失败不改写已提交 pair、隔离失败回滚 canonical；小型诊断迁入有界 quarantine，超限诊断耐久删除以避免占用无界磁盘。付费自动签名 macOS SyncConflict、state-write、DurableLocalFile 与隐私 contract 合计 77/77、0 failed/skipped/runtime warning（`/tmp/timetracker-sync-durable-state-final-20260717.xcresult`）；未启动模拟器。DerivedData、result、受测 App 与本批 owned build/test process 已在提交前清理。
- 2026-07-17 System surface 停止语义：Widget/Live Activity 的停止入口改为“打开 Time Tracker 后停止”及 app-opening symbol，仍把可见 `segmentID` 精确传入主应用；extension 不直接写 SwiftData/iCloud。付费 Apple Development 签名的 macOS `SystemSurfaceInteractionContractTests` 与 `CoreDeepLinkRoutingTests` 21/21、0 failed/skipped/runtime warning，覆盖 copy/symbol、精确 URL 与 target-only deep-link routing（`/tmp/timetracker-system-stop-copy-20260717.xcresult`）。iPhone 17 Pro / iOS 27 正常字号 demo 路径先截图确认活动计时，再用该 UUID 打开 URL；系统确实显示“Open in ‘Time Tracker’?”确认，证明入口会回到主应用而非 extension 内即时写入。此机器没有可操作的 Simulator.app 前端，未点击确认，因此不将 target mutation 伪报为实机完成。专属 UDID `8974A5B8-159C-4FCC-B67D-C02991F6D24D`、受测 App、DerivedData、result 和截图均在本批结束时删除；本批不做额外 Accessibility 专项。
- 2026-07-17 Focus task picker：计划保留小型 native menu；任务从嵌套 menu 改为 scene-owned typed searchable sheet，按标题/完整路径筛选、行显示标题/父级路径、当前选择显示勾选。选择 callback 只更新当前 Focus view 的局部 task ID，不能开始 Pomodoro 或改写 global task selection。付费 Apple Development 签名的 macOS router/Focus/AppPresentation/Pomodoro 定向回归 45/45、0 failed/skipped/runtime warning（`/tmp/timetracker-focus-picker-final-20260717.xcresult`）。新 iPhone UI XCTest 已编译至 runner，但 Xcode Beta 在 dispatch 测试前未完成 xcresult 写入；不计为 UI 通过，也没有据此宣称搜索/点击实机成功。首台测试设备 `FEF6C34B-F79E-4760-9A04-9590C02AF2AB` 受受限工具环境影响未启动测试，重试设备 `17AD41EA-D813-48D3-8393-7854FD9FF534` 亦在无 result 后停止、关闭、删除；两轮 result/DerivedData 和受测 App 均在本批结束时删除。
- 2026-07-17 SyncConflict manifest/slot state：将权威 `SyncConflictState.json` 改为 V1 小型 manifest，完整 local/forced-upload/cloud/working snapshot 写入有 byte count + SHA-256 引用的八个 A/B slot；slot 先 durable publish，manifest 为提交点，成功后回收无引用 slot。legacy inline state 首次 locked read 自动迁移；引用 slot 缺失、损坏、超限、非法 generation 或 hash 不符会 fail closed 隔离 slot 与 manifest，不能伪装成无冲突。新 conflict 不再重复写入等同 cloud 的 working baseline。付费自动签名 macOS `CoreSyncConflictStateWriteTests`、`CoreSyncConflictTests` 与 `CoreSourceLayoutTests` 102/102、0 failed/skipped/runtime warning；结果包读取后确认通过。未启动 Device Hub 或模拟器；本批没有 owned `xcodebuild`、`xctest` 或受测 App，最终 DerivedData/result 已删除。
- 2026-07-17 SyncConflict slot digest 热路径：slot writer 与 reader 改为直接 SHA-256 已有的 sorted JSON bytes，不再为同一大型快照二次 encode；`SyncDataSnapshot.fingerprint()` 保持相同 canonical digest 合同。第一次编译分别暴露参数名与测试 `try` 表达式问题，均在同一小任务中修正，失败 DerivedData 立即删除且不计验证。最终付费自动签名 macOS SyncConflict state-write/conflict/source-layout 定向回归 103/103、0 failed/skipped/runtime warning；未启动 Device Hub 或模拟器，最终 result/DerivedData 与 owned process 已删除。
- 2026-07-17 SyncConflict prompt 同锁返回：普通 lifecycle 与 Watch 已记录快照后直接消费 `recordLocalMutation` 在相同 store/state lock 内构造的 prompt，避免刚发布 manifest 后重复读写入的 slot；不需要记录快照时继续保留原有 throwing `prompt()` 语义。首轮编译发现非 `Void` helper 的返回分支和 unused-result，随后一次定向运行还揭示测试仍从 manifest root 文本查找 snapshot；三处均在同一小任务内修正，失败 DerivedData 立即删除且不计验证。最终付费自动签名 macOS SyncConflict、state-write、store serialization、System Action、Watch、source-layout 六套共 157/157、0 failed/skipped/runtime warning；未启动 Device Hub 或模拟器，结果包、DerivedData 与 owned process 已删除。
- 2026-07-17 Analytics range 语义：范围控件从相对“今天/本周/本月”改为日/周/月日历单位；历史 period 才显示“回到今天”，当前 period 不再留下禁用的重复操作。移除了未使用且会与 `AnalyticsRange` 分叉的 `TimeTrackerStore.RangePreset/selectedRange`。付费自动签名 macOS Analytics period UI contract、架构行为与三语 localization 回归 17/17、0 failed/skipped/runtime warning；未启动 Device Hub 或模拟器，结果包、DerivedData 与 owned process 已删除。
- 2026-07-19 Live Activity 可扫读身份：锁屏与 expanded Dynamic Island 复用同一个 `LiveActivityTimerRow`；新鲜普通字号的 expanded 只保留任务图标、任务名和 elapsed 单行，移除 Live Activity 可见停止入口、附加计时数量和冗余 elapsed caption，整块只打开 Today。任务身份改为复用 `TaskIdentityPresentation` 的 canonical visual/readable/abbreviated breadcrumb，并以 Unicode-safe UTF-8 上限给 ActivityKit payload 留出余量；Activity immutable segment ID、Widget/Shortcut 精确停止和八小时 stale freeze 保留。本条替代 2026-07-17 “System surface 停止语义”中 Live Activity 可见 stop 控件，Widget 行为不变。最终付费签名 macOS 系统表面/deep-link/localization/accessibility 定向回归 44/44（`/tmp/timetracker-liveactivity-mac-relevant-final-20260718.xcresult`），更新后的 source map 两项 2/2（`/tmp/timetracker-liveactivity-doc-map-final-rerun-20260719.xcresult`）；generic iOS Debug 自动签名构建 0 error/0 warning/analyzer warning（`/tmp/timetracker-liveactivity-ios-frozen-final-20260718.xcresult`），主 App、Widget、Live Activity、Watch 均为 Team `LT98S43NKA` / `Apple Development: ZEXUAN GAO (PX46M259V3)`，严格签名验证通过，主 App 保留 development APS、CloudKit 与 App Group。一次更宽 source-layout 诊断为 73/77，四项失败均是 HEAD 已存在且本 checkpoint 未修改的行数预算：`TaskDetailAnalyticsViews.swift` 189/180、`AnalyticsStore+Breakdowns.swift` 246/220、`AnalyticsCategoryDetailContent.swift` 189/180、`TaskTreeService.swift` 194/160；不把该批伪报为通过，也不把无关拆分混入本提交。专用 iPhone 17 Pro / iOS 27 simulator UI 冻结复验 1/1（`/tmp/timetracker-liveactivity-final-verification-ui-20260719.xcresult`），测试先断言 demo 只剩 `Read Apple HIG` 一条运行行，再进入系统表面，并只在审计过的 Dynamic Island simulator model 执行；compact 截图目视为图标、截断标题与 `32:13`，expanded 为图标、完整标题与 `32:19` 同排且无 Stop。锁屏截图 `/tmp/timetracker-liveactivity-lockscreen-final-019f73e0.png` 目视确认 34 pt 图标、标题/路径、时间层级且无停止指示，整卡点击已打开 Today；静态锁屏截图不能捕获系统后合成的动态秒位，因此不以图中的 `32:--` 推断用户可见计时失效，Dynamic Island XCTest 已捕获连续秒值。minimal 仅完成 source/stale contract，没有另造并发 activity 做视觉声明。锁屏批次 UDID `00705B2E-DDE6-40F3-815F-B27331709E5E` 与最终 UI 批次 UDID `CA105C82-DAB8-49FB-A35F-DAB427525E4C` 的 App/runner/extension 均已终止，设备已关闭并删除；Device Hub 已退出，全部本批 result、DerivedData、截图/attachment 临时文件已删除，最终无 owned Booted device、`xcodebuild`、`xctest`、runner 或 extension 进程。
- 2026-07-19 Blossom 原生几何触控放大：任务、分类、Checklist 与 Pomodoro 计划继续共用官方 `BlossomColorPicker` revision `9a1ee3df309e37ae271362818dcdfdb072ea9611`。iOS 薄适配层删除首版自定义的 44/88 pt 环布局与 `BlossomStyle`，改为直接使用上游默认 `ExpandedBlossomView` + `PetalLayout()`，并把完整视图按 `44 / BlossomConstants.petalSize` 统一放大；花瓣相对位置、颜色、动画、亮度弧和命中逻辑仍由上游所有。第一次 macOS 定向编译因 Core import 仍受平台条件限制而失败，修正为统一 import 后冻结源码的付费自动签名回归 48/48、0 failed/skipped/runtime warning（`/tmp/timetracker-blossom-final2-mac-20260719.xcresult`）。专用 iPhone SE (3rd generation) / iOS 27.0 正常字号 UI 用例 1/1、0 runtime warning（`/tmp/timetracker-blossom-ios-ui-final-20260719.xcresult`），实际连续点击外圈、侧圈与内圈并逐次断言非空颜色值发生变化，再次点击当前花瓣成功收起；截图保存在 `/Users/aac6fef/.codex/visualizations/2026/07/18/019f73e0-9f28-7c42-99c7-9ad324848ca0/blossom-fidelity-checkpoint-15-final`，目视确认约 301 pt 的上游花形和亮度弧完整落在 375 pt 宽屏幕内，外层为系统 popover，内部没有应用自建第二张 picker 卡片。冻结源码的 generic iOS Debug 自动签名构建通过（`/tmp/timetracker-blossom-ios-signed-final-20260719.xcresult`），主 App、Widget、Live Activity、Watch 通过严格签名与 embedded validation，均为 Team `LT98S43NKA` / `Apple Development: ZEXUAN GAO (PX46M259V3)`；主 App 保留 development APS、CloudKit 与 App Group。专用 UDID `ED8FF75E-4043-422F-82E8-4910339A2B6F` 的 App/runner 已终止，设备已关闭并删除；结果已读取后，临时 xcresult 与 DerivedData 全部删除，最终无本批 Booted device、`xcodebuild`、`xctest` 或 runner 进程。
- 2026-07-19 Watch 三页纵向 dashboard：Active Timers、Quick Start、All Tasks 使用同一个 `.verticalPage` `TabView`；第一份快照通常有 timer 进 Active、没有 timer 进 Quick，但 failure、sending/queued、connection error 或 stale attention 可覆盖默认页，后续状态不再抢页，Quick/All 顶部用 44 pt “Review/查看状态”返回 Active。Quick 排除运行项并使用 pins + usage fill；All 按 segment count、last-start、UUID 排序并保留运行项，以绿色 timer 图标标识，点按只回 Active；非运行项继续交由手机锁内并行偏好决定开始或切换。256 项/128 KiB membership 先保留实际传输的 running tasks、新 pins、旧 Watch 前四个非运行 legacy Quick rows，再以 usage remainder 补足；集合确定后才按 legacy pinned-first 重排 `recentTasks` wire，新增可选 `quickStartRank`/`allTasksRank` 保持双向兼容。冻结源码的付费自动签名 macOS 定向回归为 Watch command/projection/UI source contract 41/41、相关 source-layout 3/3、更新后 ProjectMap 入口 1/1，均 0 failed/skipped/runtime warning（`/tmp/timetracker-watch-three-page-final-core3-20260719.xcresult`、`/tmp/timetracker-watch-three-page-final-layout3-20260719.xcresult`、`/tmp/timetracker-watch-three-page-final-projectmap-20260719.xcresult`）。较早的一次宽回归真实结果为 110/115：本 checkpoint 触及的 `TimeTrackerStore+TaskReadModels.swift` 267/250 已拆出 `TaskRanking.swift` 并由最终结构回归确认；其余四项仍是 HEAD 已有且本批未改的 Analytics/Task Detail/TaskTree 行数债务（189/180、246/220、189/180、194/160），不把该批伪报为通过，也不夹带无关拆分。source-layout 下一轮又发现 `TimeTrackerStore+WatchSnapshot.swift` 为 288/250 行，命令处理已原样拆到 `TimeTrackerStore+WatchCommands.swift`；拆分后首轮 source contract 仍读旧文件而失败，修正测试 owner 后以上冻结结果通过。generic iOS Debug 与独立 generic watchOS Debug 自动签名构建均 succeeded、0 error/0 warning/analyzer warning（`/tmp/timetracker-watch-three-page-final-ios-20260719.xcresult`、`/tmp/timetracker-watch-three-page-final-watch-20260719.xcresult`）；主 App、Watch、Widget、Live Activity 均严格 codesign 通过，Team `LT98S43NKA`、Authority `Apple Development: ZEXUAN GAO (PX46M259V3)`，主 App保留 development APS、CloudKit 与 App Group。专属 Apple Watch SE 3 40mm / watchOS 27.0 simulator（UDID `FDA55A31-4EE2-4E30-982E-7312865E6773`）以 DEBUG 初始页审计入口目视覆盖 active failure、Quick attention、All running attention/普通态、Quick 无活动/等待/大字号/stale、Active 普通/空态/connection error，共 11 张截图，保存在 `/Users/aac6fef/.codex/visualizations/2026/07/18/019f73e0-9f28-7c42-99c7-9ad324848ca0/watch-three-page-checkpoint`；正常字号页面、44 pt Review、failure/status/timer 顺序、运行项返回语义与边界内换行均无裁切。该环境只有 headless CoreSimulator，不能把 DEBUG 路由截图宣称为真实 Crown 手势自动化。受测 App 已终止，专属 Watch simulator 已关闭删除，批次结束无 owned Booted device、`xcodebuild`、`xctest`、runner、App 或 extension 进程；结果包和 DerivedData 在读取审计数据后统一删除。没有配对 Apple Watch，因此 WatchConnectivity 真机往返、离线恢复、Always On 实机遮盖和功耗仍未验证。
- 2026-07-19 任务 workflow Status 移除：编辑器、任务行、Sidebar、详情、菜单、任务选择器 identity 与辅助功能值删除 Planned/Not Done/Completed 状态、Complete/Reopen 动作及完成分支准入阻断；Checklist 成为唯一产品层完成/进度语义，全部完成只让 checklist-derived remaining 归零，不锁定任务或后代。V4 `statusRaw` 不迁移、不批量回写 iCloud；legacy planned/active/completed 作为惰性兼容字节继续 round-trip，archive 读取兼容 `archivedAt` 或 raw archived，写入双 marker，活动子树仍须先停止 Timer/Pomodoro。付费自动签名 macOS Core/Lifecycle 定向回归 139/139、最终 UI/source-contract 切片 49/49，均 0 failed/skipped/runtime warning；完整 unit 诊断为 1165/1170，剩余 5 项与本 checkpoint 无关且对应生产文件和 HEAD 完全相同：四个既有行数预算（`TaskDetailAnalyticsViews.swift` 189/180、`AnalyticsStore+Breakdowns.swift` 246/220、`AnalyticsCategoryDetailContent.swift` 189/180、`TaskTreeService.swift` 194/160）及既有 `SettingsSyncSafetyContractTests.cloudActivityIsRecordedOnlyAfterConflictProcessingAndFinalRefresh` 源码契约，不将该全量批次误报为通过，也不把历史债务混入本原子提交。完整测试 target 的 macOS `build-for-testing` succeeded，0 error/0 warning/analyzer warning；三语 Localizable 各 913 keys、parity 与 `plutil` 通过。冻结源码 generic iOS Debug 自动签名构建 succeeded，主 App、Widget、Live Activity、Watch 均通过严格 codesign/embedded validation，Team `LT98S43NKA`、Authority `Apple Development: ZEXUAN GAO (PX46M259V3)`；主 App保留 development APS、CloudKit 与 App Group。本批未创建 simulator，也未声明新的截图/真机 UI 视觉验收；全部 DerivedData、xcresult 与分析临时文件在读取后删除，最终无 owned `xcodebuild`、`xctest`、runner 或 Booted device。
- 2026-07-19 Tasks/Sidebar/选择器共享摘要语法：新增唯一的 `TaskSummaryRow`，让 Tasks、Sidebar、普通/单选层级选择行与兼容 `TaskIdentityRow` 直接复用 canonical identity；正常字号标题最多两行，树内不重复 path，平铺结果保留 parent path，第二摘要行固定为 checklist、弹性间隔、被动 timer 图标、worked time 与 accessory。详情 identity 移除与 Stop 重复的 Running；计时选择器 Running 区只保留独立 Stop，停止后 sheet 不关闭且任务回到可选层级。Today/Quick Start 与 picker 复用 `TaskTimerActionButton`。iPhone 实测曾捕获外层 nominal 44 pt 但系统 bordered button 可访问 frame 仅 42.25 pt，最终以 iOS/iPadOS `.large` 原生 control size 和内部 label 最小尺寸加固，并由 rerun 逐个断言 Stop frame 至少 44×44 pt；macOS 保持 native 28 pt。冻结范围付费签名 macOS contracts 92/92（`/tmp/timetracker-task-row-scoped-final2-green-20260719.xcresult`）；一次更宽 source-layout 诊断真实为 121/125，四项失败仍是本 checkpoint 未修改的既有行数预算：`TaskDetailAnalyticsViews.swift` 189/180、`AnalyticsStore+Breakdowns.swift` 246/220、`AnalyticsCategoryDetailContent.swift` 189/180、`TaskTreeService.swift` 194/160，不将其伪报为通过或夹带拆分。正常字号 iPhone 17 Pro 与 iPad Pro 11-inch 各通过 Tasks checklist/被动 timer/长标题和 picker Stop-only/停止后重新可选两条路径，共 4/4；十张截图导出并目视通过，保存在 `/Users/aac6fef/.codex/visualizations/2026/07/18/019f73e0-9f28-7c42-99c7-9ad324848ca0/task-row-checkpoint`。generic iOS 与 macOS Debug 自动签名构建 succeeded；主 App、Widget、Live Activity、Watch 与 macOS App 均通过 `codesign --verify --deep --strict`，Team `LT98S43NKA`、Authority `Apple Development: ZEXUAN GAO (PX46M259V3)`。专属 iPhone UDID `FDEB7FC5-E052-41A5-A80E-118045D34856` 与 iPad UDID `4459E4C1-7582-4EB4-AB76-AEFF465AB7CB` 的 App/runner 已终止，设备已关闭删除；所有临时 DerivedData/xcresult 在读取后删除，最终无本批 Booted device、`xcodebuild`、`xctest`、runner、App 或 extension 进程。
- 2026-07-19 Analytics Definitions 明示口径：移除三行都不能点击却暗示帮助动作的 `info.circle`，改为区块开场、Gross/Wall/Overlap 的白话定义与计算，再用 A/B 同时运行 9:00–9:30 的同一示例核对 Gross 1h、Wall 30m、Overlap Excess 30m；三语新增键保持一致且 `plutil` 全部通过。冻结范围的付费签名 macOS UI/localization contracts 45/45；macOS focused UI runner 两次均在测试进入前启用 automation mode 超时，不计 UI 通过。正常字号 iPhone 17 Pro 定义路径 1/1；iPad Pro 11-inch 首轮在测试进入前等待 AX-loaded notification 超时，warm retry 1/1。两张截图已导出并目视确认定义完整、无假 affordance 且未被 tab chrome 遮挡，保存在 `/Users/aac6fef/.codex/visualizations/2026/07/18/019f73e0-9f28-7c42-99c7-9ad324848ca0/analytics-definitions-checkpoint-14`。generic iOS Debug 自动签名构建 succeeded；唯一 warning 是 Watch 侧无 AppIntents framework dependency 时跳过 metadata extraction，不是本 checkpoint 的源码诊断。主 App、Widget、Live Activity、Watch 和定向 macOS App 均通过严格签名验证，Team `LT98S43NKA`、Authority `Apple Development: ZEXUAN GAO (PX46M259V3)`。专属 iPhone UDID `7F7D7FB8-E46B-4DCD-A19B-F561DBF013D9` 与 iPad UDID `7256988A-6010-4D9F-93D3-63E47F65863B` 的 App/runner 已终止，设备已关闭删除；读取证据后删除所有本批 xcresult、DerivedData 与 build log，最终无本批 Booted device、`xcodebuild`、`xctest` 或 runner 进程。
- Pomodoro deadline 拆分后的主 app signed build 曾通过；Widget/Live Activity accessibility 批次的 signed builds 曾通过。
- 一次后续 targeted test bundle 在 `PomodoroActiveViews` 类型检查超时处失败；该 View 已拆分并由后续 signed build 验证可编译。该失败不能计作测试通过，也不能替代新的最终 suite。

上述证据不得机械相加成“当前工作树全套通过”。

## 8. 收口证据与未声明范围

下表是本轮终态记录。“通过”只适用于最后一次冻结范围的实际结果；“未执行”表示不作通过声明，不表示失败，也不授权 Agent 继续运行新的完整矩阵。

| 门禁 | 本轮终态 | 证据与边界 |
| --- | --- | --- |
| 完整 `timetrackerTests` | **未执行完整套件** | R1 冻结范围的签名定向测试 139/139，完整性能预算 9/9；不得把两者相加冒充完整 unit suite。 |
| 完整 `timetrackerUITests` | **未执行完整套件** | 第 7 节保留各正常字号 UI 切片及失败/卡死事实；没有冻结工作树全套 UI 通过声明。 |
| generic iOS Release 自动签名 | **本次 R1 收口未执行** | 第 7 节保留此前主 App、Widget、Live Activity、Watch 的 Team/entitlement 定向证据；不冒充 `55f19ae` 的最终 iOS archive。 |
| macOS Release 自动签名 | **通过** | `55f19ae` 对应 universal `x86_64`/`arm64` archive 成功；`Apple Development: ZEXUAN GAO (PX46M259V3)`，Team `LT98S43NKA`，hardened runtime 签名已核验。 |
| iPhone/iPad/Mac UI | **未执行冻结工作树完整矩阵** | 第 7 节保留已完成的正常字号设备切片；本次 R1 无新模拟器 UI 声明。 |
| SwiftUI/Time Profiler | **未执行最终 trace** | R1 使用 2,000 条固定 fixture 的交互性能预算：投影 `< 50 ms`、visual 后 main-actor core `< 175 ms`；这不是 Instruments trace。 |
| Simulator/进程资源释放 | **通过** | R1 最终批次未创建 simulator；结束时无 Booted device、owned `xcodebuild`/`xctest`，DerivedData、xcresult 和 archive 均已删除。 |
| 静态资源/文档 | **文档收尾通过** | `git diff --check` 与仓库内 Markdown 相对链接检查通过；本次只做文档终态一致性，不重跑独立 plist/privacy/localization 全量发行审计。 |

签名验证不得添加 `CODE_SIGNING_ALLOWED=NO`、`CODE_SIGNING_REQUIRED=NO`、空 team 或 ad-hoc identity。Simulator 的 `Sign to Run Locally` 是模拟器行为，不是设备 entitlement 证据。

## 9. 仍属真实外部/产品门禁

- Widget 真机 App Group 共享读写、timeline reload、锁屏、离线与升级。
- Watch 真机 durable queue/result、离线恢复、Always On 和功耗。
- Live Activity 真机权限、Dynamic Island、并发 activity、stale/end 与低电量。
- CloudKit 多设备并发、离线旧设备、旧/新版本同时在线与真实旧 schema fixtures。
- JSON importer、checksum、加密、预检、事务 staging/rollback 与恢复等价性。
- App Store Privacy 标签以及实际 AI endpoint 的处理/保留/训练/删除政策确认。
- 独立 Watch XCTest target、xctestplan、CI 和系统性 DocC 属于后续工程建设，不应伪装为本轮已完成。

## 10. 文档状态

- [README](../README.md)、[用户手册](UserGuide.md)、[代码文档](CodeGuide.md)、[架构](Architecture.md) 描述当前实现。
- [Agent 决策](AgentDecisions.md) 记录不可在后续重构中静默破坏的取舍。
- [隐私与安全](PrivacyAndSecurity.md) 记录数据流与发行门禁。
- [测试](Testing.md) 说明最终证据如何取得。
- [下一步计划](NextDevelopmentPlan.md) 只描述未来；日期化 UI/性能计划保留历史过程，不覆盖当前事实。
