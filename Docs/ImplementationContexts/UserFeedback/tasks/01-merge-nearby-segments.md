# 01：合并一分钟内的同任务计时段

## 反馈

> 如果同一个任务两个计时段间隔小于一分钟，自动合并为一段。

## 预期语义

- 只合并同一个稳定任务身份下、时间顺序相邻的计时段。
- “小于一分钟”按严格 `< 60 秒` 处理；恰好 60 秒不合并。
- 不合并不同任务、重叠/负间隔或无法可靠判定任务身份的数据。
- 自动合并不能绕过现有 store lock、同步冲突处理、活动计时唯一性和持久化回滚边界。
- UI、App Intent、Watch、Pomodoro 恢复等计时入口应共享同一条业务规则，不能各自复制算法。

## 验收证据

- [ ] 找到所有创建/重启计时入口和现有统一边界
- [ ] 单元测试覆盖 `< 60`、`== 60`、不同任务、活动计时与持久化失败
- [ ] 相关测试与构建通过
- [ ] `CONFIGURATION=Release scripts/build_install_all.sh` 全设备安装通过
- [ ] 终止测试 App，释放并删除本任务拥有的模拟器，确认无遗留构建/测试/扩展进程
- [ ] 回写 `Docs/userfeedback.md` 并从 `~active/` 移除链接

## 当前调查

- 状态：等待只读审计结果；尚未据提交标题判定已有实现完整。
- 依赖策略：预期使用现有 SwiftData/Swift 原生能力；若发现需要新增库，先做联网质量与许可证核验。

## 子代理交接

- `audit_timer_merge`：检查生产路径、边界测试、最近提交和缺口，不编辑文件、不占用模拟器。
