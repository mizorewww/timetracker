# Project Map

Status: current source map

Reviewed: 2026-08-02

This document is the first stop for a developer who has not worked in this repository before. It explains where code lives, which file to open first, and which boundary should own a change.

## How To Read The App

Time Tracker is organized by feature ownership and data flow:

```text
SwiftUI Feature
  -> TimeTrackerStore facade
  -> Domain command handler
  -> SwiftData repository
  -> SwiftData model
  -> Domain store snapshot
  -> Pure services derive secondary state
```

The most important rule is still: `TimeSegment` is the ledger fact. UI state, forecasts, charts, and summaries are derived from persisted task, checklist, session, segment, pomodoro, countdown, and preference models.

`TimeTrackerStore` is `@MainActor @Observable`; app roots own it with `@State`, feature views keep a plain injected reference, and binding sites use local `@Bindable`. macOS has one main `Window`; its Settings scene receives the same application store. Each visible scene separately owns its typed App-level sheet router and FIFO feedback router. CloudKit refresh enters through notification observers and the refresh planner rather than a foreground polling timer; typed activity records completed import/export/setup outcomes separately from account checks.

## Source Folders

| Path | Owns | Open this when | Do not put here |
| --- | --- | --- | --- |
| `timetracker/App` | App entry, root navigation, scenes, scene-owned typed sheet/feedback router-host pairs, CloudKit/container startup, recurrence lifecycle scheduling, build info, seed/demo data, strict/bounded deep links, weak multi-scene Watch routing, and Live Activity launch/bounded identity projection helpers | Changing app startup, platform root views, scene presentation/feedback, recurrence foreground/midnight triggers, menu commands, build metadata, deep link/Watch scene routing, or demo seeding | Screen-specific UI sections or domain algorithms |
| `timetracker/App/RootViews` | The two shells and the width decision between them: `AppRootView` retains only the compact/regular width band, `CompactShellRootView` is the tab shell, `RegularShellRootView` is the sidebar/detail split, and `DesktopRootViews` keeps one stable regular-detail navigation container | Changing shell composition, the shell breakpoint, or primary destination container lifetime. The choice is width-driven — never reintroduce an idiom or `#if os(...)` check to pick a shell | Cross-platform domain logic or feature sections |
| `timetracker/AppIntents` | Siri/Shortcuts App Intents and task entities that wrap shared command handlers, then enqueue exact post-commit events without constructing a facade/context | Adding or changing system actions such as capture Inbox item, start timer, or stop timer | Ledger/Inbox writes, SwiftUI state, duplicated command/locking logic, or synchronous system projection |
| `SharedLiveActivity` | App 与扩展共用的 Activity attributes | Changing the versioned task identity or Activity content state contract | Extension layout or ledger writes |
| `timetrackerLiveActivityExtension` | ActivityKit/Dynamic Island UI | Changing Live Activity layout, privacy, stale state, localization, or its Today open link | Durable timer facts, stop mutations, or duplicated commands |
| `timetrackerWidgetExtension` | WidgetKit entry/provider/config, active-timer layouts, supplementary/error states, and deep-link/localization/color support | Changing the widget face, widget localization, or widget target plist/entitlements | SwiftData writes, command logic, or app-only models |
| `timetrackerWatchApp` | watchOS vertical-page dashboard, Active Timers/Quick Start/All Tasks UI, timer/task/status rows, plus the `WatchAppStore` family: observable/restored state, durable command queue lifecycle, and WatchConnectivity transport | Changing Watch page navigation, task/timer presentation, localization, command retry/timeout presentation, or WatchConnectivity state | SwiftData writes, task ranking, full task-tree management, or duplicated timer logic |
| `timetracker/Models` | SwiftData models, V1...V14 schema history, migration plan, registry, deterministic recurrence/quantity identities, shared read models, and `TaskEstimatePolicy` | Adding persisted fields, migrations, shared read models, or estimate normalization | Query code, SwiftUI layout, or business workflows |
| `timetracker/Repositories` | SwiftData query/write implementations behind repository protocols, including rapid-restart canonicalization and recurrence rule/occurrence/task graph writes | Changing fetch predicates, persistence semantics, soft delete, recurrence materialization, rapid-restart tombstones, or ledger writes | UI decisions or derived analytics formulas |
| `timetracker/Repositories/ModelContext+AtomicMutation.swift` | Store/system action transaction boundary, nested save deferral, rollback | Adding a multi-step durable user action | Read-model refresh or post-commit projection |
| `timetracker/Commands` | User action handlers and use cases | Adding a durable action such as start timer, toggle checklist, move task, or update preference | SwiftUI state formatting or long-lived observable state |
| `timetracker/Stores/Facade` | `TimeTrackerStore`, first configuration, current-scene refresh/mutation lifecycle, projection recovery triggers, direct-work/parent eligibility, recurrence commands, and UI-facing facade extensions | Wiring a view action to a command handler, exposing read models, or coordinating app lifecycle | Domain-sized refresh internals, closed-app system projection, or pure algorithms |
| `timetracker/Stores/Domains` | Task, indexed ledger/session, task-scoped checklist, incremental rollup/90-day pace, analytics cache, and preference snapshots | Changing what state a feature observes after repository data changes | Button handlers, SwiftData writes, or view-specific layout |
| `timetracker/Stores/Navigation` | Shared selection and destination coordination | Changing selection invalidation or cross-platform navigation state | Feature layout or persistence writes |
| `timetracker/Stores/Refresh` | Refresh event planning and domain refresh coordination | Adding a new write event or deciding which snapshots should update | Feature UI or direct repository mutation |
| `timetracker/Services/Analytics` | Analytics aggregation, timeline layout, daily bucket cache | Changing charts, overlap math, daily/monthly summaries, or timeline lane allocation | SwiftUI chart styling that does not affect data |
| `timetracker/Services/Checklist` | Checklist draft persistence and checklist-specific editing helpers | Changing how checklist editor drafts are saved, soft-deleted, or visual metadata is preserved | Forecast formulas or task row layout |
| `timetracker/Services/Countdown` | Store-scoped countdown command coordinator | Changing countdown event command semantics | SwiftUI countdown presentation or date formatting |
| `timetracker/Services/Inbox` | Inbox suggestion state, logical identity derivation, primary/suggestion store-scoped writers, and external capture receipt replay | Changing when suggestions are shown/hidden/dismissed, how stale cross-scene writes are rejected, or how caller-owned capture keys replay | LLM networking or SwiftUI row layout |
| `timetracker/Services/Forecasting` | Explicit-estimate/checklist fallback, recursive rollups, forecast eligibility, and display selection | Changing remaining-time formulas, parent/child forecast display, or forecast explanations | Checklist editing UI |
| `timetracker/Services/Instrumentation` | Performance signpost names and measured intervals | Adding an Instruments-visible boundary around domain refresh or calculation work | Product analytics, user data logging, or business rules |
| `timetracker/Services/LLM` | OpenAI-compatible endpoint validation, credential-safe transport, response decoding, suggestion services, complete-workspace task-planning tools, and in-memory proposal execution | Changing model discovery, redirect policy, concurrency/backoff, suggestion/task-plan prompt contracts, tool schemas/round trips, or response decoding | Keychain persistence, SwiftData writes, or SwiftUI presentation state |
| `timetracker/Services/Ledger` | Device-local CloudKit startup mode, persistence-write diagnostics, timer command DTOs, duration formatting, summary, and gross/wall-clock aggregation | Changing device-local sync startup, transaction save diagnostics, or shared time math | SwiftData fetches or view layout |
| `timetracker/Services/Preferences` | Store-scoped synced preference writes, device-only credential coupling, and device-local macOS shortcut persistence | Changing preference commit serialization, Keychain/persisted-configuration ordering, or macOS shortcut payload loading | Preference JSON normalization or Settings view layout |
| `timetracker/Services/Maintenance` | Database repair and cleanup support | Changing optimization or repair safety | Normal timer/task write flows or export presentation |
| `timetracker/Services/SystemIntegration` | Sync-conflict orchestration/state/export, record-level LWW auto-merge, versioned data snapshots, four-lane committed-mutation scheduling, SwiftData persistent-history driving/impact mapping, durable lane cursor/reset fencing, background sync/system-surface materialization, async prompt broadcasting, queued in-process convergence, durable local-file/lock primitives, credentials, App Group/cache DTOs, Watch command processing, and WatchConnectivity payload/transport | Changing conflict recovery, automatic conflict merging, post-commit sync/Widget/Watch/Live Activity projection, local recovery files, export/snapshot mappings, Widget-visible state, Watch command idempotency, or extension handoff data | Extension UI or durable ledger writes |
| `timetracker/Services/Tasks` | Task tree validation, canonical metadata repair, title paths, descendants, direct-work/parent eligibility, recurrence rule/materialization safety, archive/restore, legacy tombstone compatibility, flat visible rows, AI workspace capture/overlay models, and store-scoped atomic AI proposal application | Changing task nesting, recurrence current-day generation, archived or historical-tombstone availability, old `statusRaw` handling, orphan/cycle repair, legal parent choices, sidebar/tasks row derivation, AI workspace identity/CAS, or atomic mixed task-plan validation | View-local draft state or provider networking |
| `timetracker/Services/TimeTracking` | Pure canonical active-segment snapshots, deterministic start/stop admission plans, and the ordinary rapid-restart eligibility policy | Changing same-task reuse/replacement, sub-minute restart eligibility, exclusive/parallel stop scope, exact/task/current stop semantics, or stable timer tie-breaks | SwiftData fetch/write, file locking, UI refresh, or post-commit projection |
| `timetracker/Features/Home` | Today screen composition | Changing Today summary, active timers, quick start, forecast, timeline, or countdown presentation | Cross-screen components that should be reused |
| `timetracker/Features/Inbox` | Inbox capture, open/completed rows, suggestion feedback, and suggestion draft UI | Changing capture/list presentation, row actions, or suggestion review flows | Inbox persistence, LLM networking, or task-conversion rules |
| `timetracker/Features/Tasks` | Task browsing and the unified task workspace | Changing task list rows, workspace evidence/editing, editor fields, checklist editing, icon/color picking, or parent selection UI | Time ledger algorithms |
| `timetracker/Features/Analytics` | Analytics screen composition | Changing analytics tabs, chart sections, forecast list presentation, or timeline chart UI | Analytics math that can be unit tested in services |
| `timetracker/Features/Pomodoro` | Pomodoro setup, active run, recent ledger UI | Changing pomodoro screen layout or controls | Pomodoro ledger writes; use commands/repositories |
| `timetracker/Features/Settings` | Settings form, macOS shortcut recorder, and support rows | Changing user preferences, shortcut recording, export UI, sync controls, maintenance UI, or About display | Preference persistence codecs or CloudKit startup |
| `timetracker/Features/Sidebar` | App navigation sidebar | Changing navigation row presentation, sidebar task tree display, or split-view navigation behavior | Task tree algorithms |
| `timetracker/Features/Ledger` | Manual time entry and segment editing UI | Changing manual entry sheets or ledger row editor UI | Ledger query semantics |
| `timetracker/Shared` | Extension-safe shared helpers and app strings | Changing localization access or non-UI helpers shared by app/extension | Feature-specific components |
| `timetracker/SharedUI/Foundation` | Design tokens, colors, layout policies, responsive breakpoints | Changing spacing, card metrics, platform breakpoints, or shared visual constants | Feature-specific row contents |
| `timetracker/SharedUI/Components` | Reusable native-styled controls, task identity/summary rows, timer action buttons, metric cards, and info popovers | Reusing a control in two or more features or changing the cross-surface task-row grammar | One-off feature layout that has no second caller, or timer admission policy |

## Common Change Entry Points

| Task | Start here | Then check |
| --- | --- | --- |
| Start/stop timer behavior, including rapid restart | `Services/TimeTracking/TimerAdmissionPolicy.swift` and `TimerRapidRestartPolicy.swift`, then `Services/TimeTracking/StoreScopedTimerCommandCoordinator.swift` | `Repositories/SwiftDataTaskRepository+Queries.swift` (`directWorkTask(id:)` bounded admission), `SwiftDataTimeTrackingRepository+RapidRestart.swift`, `SwiftDataTimeTrackingRepository+Mutations.swift`, `SwiftDataTimeTrackingRepository+SegmentStop.swift`, `Models/PersistentModelDeduplication.swift`, `Services/SystemIntegration/SyncDataSnapshot+RestoreLedger.swift`, `Commands/TimerCommands.swift`, `TimerAdmissionModels.swift`, `Stores/Domains/LedgerStore.swift`, `Services/Ledger/TimerCommand.swift`, Pomodoro replacement semantics, SystemAction/Watch/AppIntent/deep-link writers, and coordinator lock/fresh-context tests |
| Manual time entry or segment edit | `Commands/LedgerCommands.swift` | `Features/Ledger`, `Stores/Domains/LedgerStore.swift`, `Stores/Domains/LedgerStore+SegmentIndex.swift` (day/change index), `Stores/Domains/LedgerStore+FlatSegmentIndex.swift` (ordered flat-array index), Pomodoro invariants, analytics invalidation tests |
| Tracked-time interval, future-time, or clock-skew semantics | `Models/LedgerModels.swift` (`TrackedTimePolicy`) | time repository query/mutation extensions, `Services/Ledger/TimeAggregationService.swift`, Analytics/Forecasting services, Ledger/Rollup indexes, timeline/formatter call sites, future-time and clock-rewind tests |
| Task create/edit/move/archive/restore | `Commands/TaskCommands.swift` | `Models/TaskModels.swift` (`LegacyTaskStatusRaw` and tombstone compatibility), `Services/Tasks/TaskTrackingAvailabilityService.swift`, the `TaskTree*` service/index/projection family, `Services/Tasks/TaskHierarchyMetadataService.swift`, `Stores/Facade/TimeTrackerStore+TaskCommands.swift`, repository hierarchy/mutation files, `Features/Tasks` |
| Daily task recurrence materialization | `Services/Tasks/StoreScopedTaskRecurrenceCommandCoordinator.swift` and its `+RuleMutations` / `+Materialization` extensions | `Models/TaskRecurrenceModels.swift`, `Services/Tasks/TaskRecurrenceMutationModels.swift`, `Services/Tasks/TaskRecurrencePersistenceState.swift`, `Repositories/SwiftDataTaskRepository+Recurrence.swift`, `Stores/Facade/TimeTrackerStore+TaskRecurrenceCommands.swift`, `App/TaskRecurrenceLifecycleModifier.swift`, direct-work admission and recurrence safety tests |
| Task categories | `Stores/Facade/TimeTrackerStore+TaskCategoryCommands.swift` | `Models/TaskModels.swift`, `Repositories/SwiftDataTaskRepository+Categories.swift`, `Features/Tasks`, `Features/Sidebar` |
| Checklist UI or persistence | `Commands/ChecklistCommands.swift` | `Stores/Domains/ChecklistStore.swift`, `Features/Tasks/Editor`, `Features/Tasks/Detail/TaskDetailWorkspace.swift`, `TaskDetailContentView.swift`, `Services/Checklist/ChecklistDraftService.swift`, `Services/Checklist/StoreScopedChecklistCommandCoordinator.swift`, `Services/Checklist/StoreScopedChecklistCommandCoordinator+VisualSuggestions.swift`, `Services/Forecasting/TaskRollupService.swift` |
| Task/category/checklist/Pomodoro symbol or color UI | `Features/Tasks/Editor/SymbolColorWell.swift` and `SymbolColorPickerButton.swift` | `MacBlossomColorPresenter.swift` owns only macOS anchor/window presentation; `SymbolColorPickerView.swift`, `SymbolCatalog.swift`, shared draft callers, Blossom Core/package contracts, and platform UI tests |
| In-process multi-window read-model convergence | `Services/SystemIntegration/StoreMutationBroadcaster.swift` | `Stores/Facade/TimeTrackerStore+Lifecycle.swift`, `TimeTrackerStore+SyncObservers.swift`, `Stores/Refresh/StoreRefreshPlanning.swift`, `Stores/Refresh/StoreRefreshCoordinator.swift`; delivery is queued to the next MainActor turn and consumers stay read-only; persistent history/CloudKit remain the cross-process source of truth |
| Committed mutation post-commit projections | `Services/SystemIntegration/CommittedMutationSystemProjectionScheduler.swift` | `CommittedMutationSystemProjectionWorker.swift`, `CommittedMutationSystemSurfaceMaterializer.swift` (single background DTO materialization), `PersistentHistorySyncSnapshotWorker.swift`, `PersistentHistoryProjectionDriver.swift` (non-MainActor history paging/effect acknowledgement), `PersistentHistoryProjectionImpact.swift` (entity-name → conservative domain invalidation), `PersistentHistoryLaneCursorStore.swift` (four durable frontiers/full attempts/reset epoch), `Commands/SystemActionCommands.swift`, `Stores/Facade/TimeTrackerStore+Lifecycle.swift`, `TimeTrackerStore+WatchCommands.swift`, lifecycle recovery triggers, Widget/Watch/Live Activity adapters, and scheduler/worker/boundary tests |
| Forecast math / incremental rollup | `Models/TaskEstimatePolicy.swift`, `Services/Forecasting/TaskRollupService.swift` | forecast helper/resolution files, `Stores/Domains/RollupIncrementalIndex.swift` (state/full rebuild), `RollupIncrementalIndex+Mutation.swift` (scoped deltas/replacements), topology, pace, and activity extensions, `Services/Forecasting/TaskRollupCalculationContext.swift`, `Services/Forecasting/ForecastDisplayService.swift` |
| SwiftData schema migration | `Models/SchemaModels.swift`, `Models/SchemaMigrationPlan.swift` | `Models/TimeTrackerModelRegistry.swift`, legacy model declarations, `timetrackerTests/Support/*SchemaCompatibilityFixtures.swift`, lifecycle migration tests |
| Analytics chart data and cache | `Stores/Domains/AnalyticsStore.swift` | `Stores/Facade/TimeTrackerStore+Analytics.swift` (sync snapshot/cache lifecycle), `TimeTrackerStore+AnalyticsLoading.swift` (async cache-miss visual loading), `TimeTrackerStore+AnalyticsReadModels.swift` (UI-facing projections), `TimeTrackerStore+LedgerRecentRecords.swift`, `Stores/Domains/AnalyticsStore+Caching.swift` (cache-aware daily assembly), `AnalyticsStore+ComparisonWindow.swift`, `AnalyticsStore+SnapshotBuilding.swift`, `AnalyticsStore+DecisionSupport.swift`, `AnalyticsStore+Insights.swift`, `AnalyticsStore+Metrics.swift`, `AnalyticsStore+TaskSnapshot.swift`, `AnalyticsStore+TaskRecentRecords.swift`, `Stores/Domains/LedgerStore+RecordIndexes.swift`, `Stores/Domains/LedgerStore+SegmentQueryIndex.swift`, breakdown extensions, `AnalyticsStore+Overlap.swift` (legacy/reference facade), `AnalyticsStore+OverlapSweep.swift`, `AnalyticsStore+OverlapMaterialization.swift`, `AnalyticsStore+OverlapParticipants.swift`, `Services/Analytics/AnalyticsVisualSnapshotModels.swift`, `AnalyticsVisualSnapshotService.swift`, `AnalyticsVisualOverlapService*.swift` (Sendable Today visual projection/worker), `Services/Analytics/AnalyticsEngine.swift`, `Services/Analytics/LedgerBucketCache.swift`, `Features/Analytics` |
| Analytics category or standalone navigation and detail composition | `Features/Analytics/AnalyticsViews.swift` | `AnalyticsCategory.swift` (category plus standalone typed destinations), `AnalyticsHeatmapView.swift` (shared Heatmap page), `AnalyticsCategoryDetailView.swift` (request/load), `AnalyticsCategoryDetailContent.swift` (section routing), `Sections/AnalyticsFocusRoundViews.swift`, period/metric/detail-list files, `Sections/AnalyticsDistributionSlice.swift`, `Sections/AnalyticsGroupBreakdownPresentation.swift`, `Sections/AnalyticsGroupBarLayout.swift`, Analytics timeline and UI/source-layout contracts |
| Pomodoro setup UI | `Features/Pomodoro/Sections/PomodoroSetupViews.swift` | `PomodoroSetupEmptyState.swift`, `PomodoroFocusSetupControls.swift`, `PomodoroSetupSelectionViews.swift`, `PomodoroTimerFace.swift`, active-run and ledger sections, Pomodoro UI/source-layout contracts |
| Today layout or live-resize performance | `Features/Home/HomeViews.swift` | `SharedUI/Foundation/LayoutPolicies.swift` (`HomeLayoutPolicy`, `HomeViewportMeasurement`), `Features/Home/Sections`, `Features/Home/Rows`, resize policy tests, macOS SwiftUI Instruments |
| Inbox capture and review UI | `Features/Inbox/InboxViews.swift` | `Services/Inbox/StoreScopedInboxCommandCoordinator.swift`, `StoreScopedInboxCommandCoordinator+CaptureReceipts.swift`, `Services/Inbox/StoreScopedInboxSuggestionCommandCoordinator.swift`, `Commands/InboxCommands.swift`, `Commands/InboxCaptureCommand.swift`, Inbox store/facade files, suggestion commands/services, `Features/Inbox/InboxListView.swift`, row/editor files |
| Shared task identity or summary-row layout | `SharedUI/Components/TaskSummaryRow.swift` | `TaskIdentityRow.swift`, `Services/Tasks/TaskIdentityPresentation.swift`, Tasks management rows, Sidebar task tree, `TaskHierarchyPickerRows.swift`, shared-component/task/picker UI contracts |
| Shared Start/Switch/Stop control presentation | `SharedUI/Components/TaskTimerActionButton.swift` | `Features/Home/Rows/HomeTimerRows.swift`, `TaskHierarchyPickerRows.swift`, `Commands/TimerPickerCommands.swift`, timer-picker/Home UI contracts; durable semantics remain in the timer command boundary |
| Task hierarchy picker layout or selection semantics | `SharedUI/Components/TaskHierarchyPicker.swift` | `TaskHierarchyPickerRows.swift`, `TaskHierarchyPickerBehavior.swift`, `TaskHierarchyProjection.swift`, `TaskTimerActionButton.swift`, scene presentation host/router, Pomodoro and Inbox callers, picker/UI tests |
| Unified task detail/edit workspace | `Features/Tasks/Detail/TaskDetailWorkspace.swift` | `TaskDetailWorkspace+Navigation.swift`, `TaskDetailWorkspace+Recovery.swift`, `TaskDetailContentView.swift`, identity/overview/analytics/record/availability sections, `TaskNotesMarkdownPreview.swift`, `Features/Tasks/Editor`, `Stores/Facade/TimeTrackerStore+TaskReadModels.swift` |
| Settings | `Features/Settings/SettingsViews.swift` | display/timing, Pomodoro, picker, countdown, sync, data, `LLMSettingsViews.swift`, macOS `MacKeyboardShortcutSettingsSection.swift`, bindings/actions/support, `Commands/PreferenceCommands.swift` |
| macOS menu shortcuts | `Models/MacKeyboardShortcutModels.swift` | `Commands/MacKeyboardShortcutPreferenceCommand.swift`, `Services/Preferences/UserDefaultsMacKeyboardShortcutPreferenceStore.swift`, `Services/Preferences/MacKeyboardShortcutSettings.swift`, `App/TimeTrackerCommands.swift`, local `Packages/MacKeyboardShortcuts`, preference/UI tests |
| App-level sheet or transient error | `App/AppPresentationRouter.swift` or `App/AppSceneFeedbackRouter.swift` | matching host, `ContentView.swift`, `SettingsSceneView.swift`, initiating feature's throwing/typed boundary, presentation/feedback contract tests; never store a new scene-local draft or alert queue in `TimeTrackerStore` |
| Reusable Settings row, input, action, or sync feedback | `SharedUI/Components/SettingsRows.swift` | `SettingsActionRows.swift`, `SettingsInputRows.swift`, `SettingsPresentationModifiers.swift`, `SettingsSyncFeedbackRow.swift`, SharedComponents/UI contracts, Settings call sites |
| iCloud/user settings sync | `Commands/PreferenceCommands.swift` | `Models/SyncedPreferences.swift`, `Models/SyncedPreferenceMigrations.swift`, `Models/SyncActivityModels.swift`, `Stores/Domains/PreferenceStore.swift`, `Stores/Facade/TimeTrackerStore+SyncObservers.swift` (typed activity outcome), `Services/SystemIntegration/SyncConflictService.swift`, local/cloud/recovery/resolution extensions, `SyncConflictService+StateLock.swift` (file lock), `SyncConflictService+State.swift` + `SyncConflictService+StateWriting.swift` (manifest persistence/file protection), `SyncConflictService+SnapshotSlots.swift` + `SyncConflictService+SnapshotSlotLocations.swift` + `SyncConflictStateManifest.swift` (bounded A/B snapshot authority), `SyncConflictState.swift` (runtime epoch/generation/checkpoints), snapshot capture/domain restore/record families, `App/AppModelContainerFactory.swift`, `App/AppModelContainerFactory+Fallback.swift` (fallback snapshot/reset outer lock); iCloud enablement and account availability are separate device-local concerns |
| Durable local recovery file or queue | `Services/SystemIntegration/DurableLocalFile.swift` | `PathFileLock.swift`, `DurableLocalFile+Paths.swift`, `+Writing.swift`, `+Synchronization.swift`, `+Quarantine.swift`, `+QuarantinePruning.swift`; use one stable explicit durable root per state family and keep JSON/domain validation in the caller |
| Demo data seeding or clearing | `App/AppDemoDataConfiguration.swift` | `App/AppModelContainerFactory.swift`, `App/SeedData.swift`, demo build/cleanup files, lifecycle tests; Debug/Release default off and demo store is separate. Writes additionally require `allowsDemoDataMutation` (DEBUG **and** the isolated demo store is open) |
| Marking or clearing non-user rows | `App/SyntheticDataOrigin.swift` | The one registry of synthetic `deviceID` values (`demo`, `cloud-smoke`, `ui-test`). Producers stamp one; `App/SeedData+Cleanup.swift` and `Features/Settings/SettingsViewActions.swift` match via `marks(_:)`. A new kind of synthetic data is only removable once registered here |
| Keeping test runs out of the installed app's state | `App/AppRuntimeEnvironment.swift` | `AppRuntimeEnvironment.isTestHost` / `namespaced(_:)` and `AppDefaults.shared`. App code and tests must never use `UserDefaults.standard` directly — the unsandboxed macOS test host shares the installed app's domain. Also namespaces `Services/SystemIntegration/SyncConflictService+StateLocations.swift` and the `Shared/WidgetSnapshotStore.swift` App Group suite; gate is `timetrackerTests/Lifecycle/TestHostIsolationTests.swift` |
| AI model configuration | `Features/Settings/LLMSettingsViews.swift` | `Services/LLM/LLMModelService.swift`, `Services/LLM/LLMPromptCatalog.swift` shared request policy, `Services/SystemIntegration/LLMCredentialStore.swift`, `Models/SyncedPreferences.swift` endpoint/model/reasoning-effort preferences, `Stores/Facade/TimeTrackerStore+LLMPreferenceCommands.swift` |
| AI Inbox/checklist suggestions | `Services/LLM/LLMInboxSuggestionService.swift` and `Services/LLM/LLMChecklistVisualSuggestionService.swift` | `Services/LLM/LLMSuggestionInputPolicy.swift`, `Features/Tasks/Editor/SymbolCatalog.swift` complete canonical symbol catalogue, Inbox/checklist facade suggestion files, `Services/Inbox/StoreScopedInboxSuggestionCommandCoordinator.swift`, `Services/Inbox/InboxSuggestionStateService.swift`, `Services/Inbox/InboxSuggestionIdentityService.swift`, complete-request/concurrency/backoff/identity tests, privacy documentation |
| AI category/task/checklist workspace planning | `Services/LLM/LLMTaskWorkspacePlanningService.swift` and its `+Schemas` / `+Tools` extensions | `Services/Tasks/AITaskWorkspaceModels.swift`, `AITaskWorkspaceOverlay.swift`, `AITaskAtomicMutationModels.swift`, `StoreScopedAITaskAtomicMutationCoordinator.swift`, `Features/Tasks/Generation/AITaskWorkspacePlanGeneratorViews.swift` for request/workflow composition, the adjacent `AITaskWorkspaceReviewPresentation.swift`, `AITaskWorkspaceReviewPolicy.swift`, and `AITaskWorkspaceReviewViews.swift` for independently tested review safety and rendering, `Stores/Facade/TimeTrackerStore+AITaskPlanCommands.swift`, OpenAI tool-call DTO/assembler files, task-plan instruction preference/editor, workspace/tool/mutation/UI tests, and privacy documentation; the replaced create-only network and mutation stack has been removed and must not be restored as a fallback |
| Performance signposts and profiling | `Services/Instrumentation/PerformanceSignpost.swift` | bounded query/incremental correctness tests where observable, `Docs/Testing.md`, the active implementation memory under `Docs/ImplementationContexts/`, and seeded Release trace evidence in git history |
| Apple Health local replica | `App/AppleHealthReplicaModelContainerFactory.swift` | `Models/AppleHealthReplicaSchema.swift`, `Models/AppleHealthReplicaModels.swift`, `Repositories/AppleHealthReplicaRepository.swift`, `Services/SystemIntegration/AppleHealthReplicaSyncService.swift`, `Services/SystemIntegration/AppleHealthDataReader.swift` (anchored + observer/background delivery), `Stores/Facade/TimeTrackerStore+AppleHealthTimeline.swift` (dirty generation + projection revision), iOS entitlement, schema/repository/sync/facade tests; never add these models to the main registry or Cloud snapshot |
| JSON export | `Stores/Facade/TimeTrackerStore+MaintenanceCommands.swift` | `Services/SystemIntegration/UserDataExport.swift`, `Features/Settings/Support/SettingsExportDocument.swift`, typed business/Health envelope and fail-closed tests |
| Siri/Shortcuts App Intents | `AppIntents/TimeTrackerAppIntents.swift` | `Commands/SystemActionCommands.swift`, the owning store-scoped coordinator, `SystemActionPostCommitEffects`, shared projection scheduler/worker, queued scene broadcaster, localization/discovery tests |
| Deep links from Widget or system surfaces | `App/AppDeepLinkRouter.swift` (`PendingDeepLinkQueue` included) | `Stores/Facade/TimeTrackerStore+DeepLinks.swift`, `App/ContentView.swift`, Widget/Live Activity link builders, `timetracker/Info.plist`, routing tests |
| Startup/foreground/remote-import projection recovery | `Stores/Facade/TimeTrackerStore+Configuration.swift` | `TimeTrackerStore+RefreshLifecycle.swift`, `TimeTrackerStore+SyncRefreshPipeline.swift`, shared scheduler/worker/driver, sync observers, App container factory, startup full catch-up and forced-Watch tests |
| Widget/Watch snapshot projection or limits | `Services/SystemIntegration/CommittedMutationSystemSurfaceMaterializer.swift` | `WidgetSnapshotCache.swift`, `Shared/WidgetSnapshotModels.swift` (Unicode-safe shaping/validation), `Shared/WidgetSnapshotStore.swift` (256 KiB App Group boundary), `Stores/Facade/TimeTrackerStore+WatchSnapshot.swift` (pure compatibility oracle), `Stores/Facade/TimeTrackerStore+TaskRanking.swift` (shared segment-count/last-start/UUID order), `Shared/WatchStateSnapshotModels.swift` (128 KiB text/64 active/256 `recentTasks`, `legacyQuickStartTaskLimit`, legacy pinned-first wire order plus optional `quickStartRank`/`allTasksRank` compatibility), extension targets and snapshot/materializer tests |
| Widget extension UI | `timetrackerWidgetExtension/TimeTrackerWidget.swift` | `ActiveTimerWidgetView.swift`, `WidgetSupplementaryViews.swift`, `WidgetSupport.swift`, `Shared/WidgetSnapshotModels.swift`, widget localization files, WidgetKit target settings |
| Watch timer command handoff | `Services/SystemIntegration/WatchCommandProcessor.swift` | `App/WatchCommandRouter.swift` (weak multi-scene dispatch), `Shared/WatchCommandModels.swift` (queue/result state), `timetrackerWatchApp/WatchAppStore.swift` (observable state/restore), `WatchAppStore+Commands.swift` (enqueue/retry/discard/Watch-local 20 s timeout/persistence), `WatchAppStore+Connectivity.swift` (activation/transport/payload/freshness), `WatchAppStore+SessionDelegate.swift` (WCSession callbacks), `Stores/Facade/TimeTrackerStore+WatchCommands.swift` (exact mutation events plus same-receipt `forcedSystemSinks: [.watch]` for every iPhone-handled outcome), `Stores/Facade/TimeTrackerStore+WatchSnapshot.swift` (pure projection oracle), payload codec/bridge, timer commands |
| Watch app UI | `timetrackerWatchApp/WatchDashboardView.swift` (one-shot timer-or-attention initial page selection plus 44 pt Quick/All attention return) | `WatchActiveTimersPage.swift` (failure/status/timer order), `WatchTaskListView.swift`, `WatchTaskRows.swift`, `WatchTimerRows.swift`, `WatchStatusViews.swift`, `WatchColorSupport.swift`, the four-file `WatchAppStore` family (`WatchAppStore.swift` plus Commands, Connectivity, and SessionDelegate extensions), `Stores/Facade/TimeTrackerStore+TaskRanking.swift`, `Shared/WatchCommandModels.swift`, `Shared/WatchStateSnapshotModels.swift`, watch localization files |
| Live Activity display (read-only)/open link | `timetrackerLiveActivityExtension` | `SharedLiveActivity/TimeTrackingActivityAttributes.swift`, `App/LiveActivityProjection.swift`, `App/AppDeepLinkRouter.swift`, `App/TimeTrackerLiveActivities.swift` |
| Localization | `Shared/AppStrings.swift` | every target's `*.lproj/Localizable.strings` and `*.lproj/InfoPlist.strings`, main-app `*.lproj/AppShortcuts.strings`, localization parity tests |

## Placement Rules

1. Put durable write behavior in `Commands`, not in SwiftUI button closures.
2. Put SwiftData fetch/write implementation in `Repositories`, not in feature views.
3. Put testable calculations in `Services`, not in `body`.
4. Put screen-specific composition in `Features/<Feature>`.
5. Put shared styling and controls in `SharedUI` only when at least two features use or are about to use them.
6. Keep `TimeTrackerStore` as a facade. If a method grows domain logic, move that logic into a command handler, domain store, or service.
7. If a directory starts collecting unrelated files, split it before adding more.
8. After moving files, run the scheme visibility check, macOS tests, and generic iOS build from `Docs/Testing.md`.
9. For schema changes, prefer additive extension models over changing core ledger/task models. Update `Docs/Architecture.md` schema rules and add migration/compatibility tests before UI work.

## Naming Rules

- `*Commands.swift`: durable write actions and use-case-style command handlers.
- `*Store.swift`: observable snapshots and refresh logic for one domain.
- `*Service.swift`: pure calculations or maintenance helpers that can be unit tested without SwiftUI.
- `*Views.swift`: SwiftUI composition for one feature or one section group.
- `*RowViews.swift`: reusable rows inside one feature.
- `*SupportViews.swift`: small support controls that are not the feature's main screen.
- `TimeTrackerStore+*.swift`: facade extensions only; these live in `Stores/Facade`.

## Before Adding A Feature

1. Add expected behavior to `Docs/Architecture.md`, this map, or a focused feature document.
2. Add or update tests for the service, command, store, or UI contract boundary.
3. Implement the smallest domain owner first.
4. Wire SwiftUI last.
5. Run the baseline checks listed in `Docs/Testing.md`.

## Planning Documents

Use these before starting larger work:

| Document | Purpose |
| --- | --- |
| `Docs/NextDevelopmentPlan.md` | Product backlog and feature acceptance criteria for the next development cycles. |
| `Docs/CodeRefactorPlan.md` | Current concentration inventory and architecture guardrails. |
| `Docs/UI-Design.md` | Native-first UI guardrails and review/screenshot checklists. |
